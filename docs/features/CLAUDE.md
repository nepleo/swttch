# 기능 문서 색인 (Feature Docs Index)

이 폴더는 사용자를 독자로 한 **기능 설명서**들을 모아둔다. 작성 규칙은 [docs/CLAUDE.md](../CLAUDE.md)를 따른다.

각 기능은 `NNN-feature_name/` 폴더로 관리하며, 폴더 안에 언어별 번역본(`en.md`, `ko.md`, …)을 둔다.

## 색인

- [Editor Context Tag](./001-editor_context_tag/en.md) — 에디터에서 연 파일이나 선택한 코드를 자동으로 채팅 컨텍스트로 첨부하는 토글 태그 ([#122](https://github.com/Swttch/swttch/issues/122), [#133](https://github.com/Swttch/swttch/pull/133))
- [Multi-Account Management](./002-multi_account_management/en.md) — 여러 Claude 계정을 저장하고 전환하며, 계정별 사용량을 한 화면에서 비교하는 멀티 계정 기능 ([#134](https://github.com/Swttch/swttch/pull/134))
- [MCP Server Management](./003-mcp_server_management/en.md) — GUI에서 MCP 서버를 조회·추가·편집·제거·활성화·재연결하는 MCP 서버 관리 패널 ([#136](https://github.com/Swttch/swttch/pull/136))
- [Settings Overlay](./004-settings_overlay/en.md) — 설정을 현재 채팅 위 오버레이로 열어 진행 중인 세션을 잃지 않게 하고, 여는 방식(오버레이/새 탭)을 선택할 수 있는 기능 ([#137](https://github.com/Swttch/swttch/pull/137))
- [Claude Code CLI Version & Update](./005-cli_version_update/en.md) — 설치 방식에 맞는 CLI 업데이트와, 이미 설치된 extend-kit 의존성의 시작 시 자동 업데이트.
- [Effort & Fast Mode](./006-effort_and_fast_mode/en.md) — 모델별로 추론 깊이(Effort 슬라이더·Ultracode)와 빠른 출력(Fast mode)을 조절하는 Model 섹션 컨트롤. 지원하지 않는 모델에서는 숨기지 않고 비활성+툴팁으로 안내 ([#121](https://github.com/Swttch/swttch/issues/121), [#152](https://github.com/Swttch/swttch/issues/152))
- [Native rendering for JetBrains IDE MCP tools](./007-jetbrains_mcp_native_rendering/en.md) — IDE 내장 MCP 도구(`mcp__idea__…` 등) 호출을 raw JSON 대신 전용 채팅 카드로 렌더링하고, `file:line` 이동·인간화된 권한 다이얼로그·중립 declined 상태·프로젝트 확인 칩·정직한 상태 점을 제공하는 기능 ([#147](https://github.com/Swttch/swttch/pull/147))
- [Usage breakdown in the account modal](./008-usage_report/en.md) — `/usage`를 사용량 모달로 연결하고, `claude -p "/usage"`의 상세 분석(기간별 요청/세션 수·인사이트·상위 skills/subagents/plugins/MCP)을 커서처럼 UI로 렌더링. 계정 전환에 반응 ([#148](https://github.com/Swttch/swttch/issues/148))
- [Interface Language (UI translations)](./009-interface_language_i18n/en.md) — GUI 전체를 10개 언어로 번역하고, Claude 응답 언어(자유 텍스트)와 분리된 "Interface Language" 설정을 신설. 중국어 간체/번체 분리, 현지어 라벨, 컨트리뷰터용 `add-locale` 스킬 포함 ([#141](https://github.com/Swttch/swttch/issues/141), [#160](https://github.com/Swttch/swttch/pull/160))
- [RTL (right-to-left) language support](./010-rtl_support/en.md) — 설정 → General에 "RTL(Right-to-left) 켜기" 토글을 추가해 화면 전체를 오른쪽 기준으로 미러링하고, 인터페이스 언어에 فارسی(페르시아어)·العربية(아랍어)를 추가. 언어를 RTL 언어로 바꾸면 자동으로 켜지되(그 반대도) 이후엔 수동 조작 가능, 코드/터미널 출력/수식은 항상 LTR로 보존 ([#158](https://github.com/Swttch/swttch/issues/158))
- [Context usage in chat](./011-context_command_in_chat/en.md) — `/context` 슬래시 명령을 GUI 채팅에서 정상 실행하고, 터미널 TUI처럼 컬러 격자 기반 컨텍스트 사용량 카드로 렌더링 ([#196](https://github.com/Swttch/swttch/issues/196), [#198](https://github.com/Swttch/swttch/pull/198))
- [Editor context follows the focused panel](./012-editor_context_focused_panel/en.md) — 여러 Claude 패널/탭을 열어도 파일 뱃지와 `Alt+K` 멘션이 마지막으로 포커스한 패널로만 가도록 라우팅. 새 탭/브라우저의 페어링 세션 재사용(403 방지), 툴윈도우 `Alt+K` 새탭 방지, 마지막 포커스가 브라우저면 IDE 미개입 포함 ([#180](https://github.com/Swttch/swttch/issues/180), [#199](https://github.com/Swttch/swttch/issues/199), [#205](https://github.com/Swttch/swttch/issues/205), [#207](https://github.com/Swttch/swttch/pull/207))
- [Adjustable chat line spacing](./014-line_spacing/en.md) — 채팅 메시지 본문 텍스트(문단·리스트·헤딩·인용·표)의 줄 간격을 설정 → Appearance → Theme의 글꼴 크기 아래 "줄 간격" 숫자 입력(0.5~10, 0.1 단위, 기본 1.6)으로 조절. 값이 없으면 기존 1.6로 폴백해 회귀 없음, 코드 블록·인라인 코드는 자체 고정 간격 유지 ([#218](https://github.com/Swttch/swttch/issues/218), [#221](https://github.com/Swttch/swttch/pull/221))
- [Clickable jump-to-line file references in chat](./013-clickable_file_references/en.md) — Claude 답변 속 파일 참조를 클릭하면 IDE에서 해당 라인(·컬럼)으로 열리게 하는 기능. Markdown 파일 링크, 문장 속 평문 `path:line`/`:line:col`/`#L…` 표기, `@` 멘션 칩의 라인 이동을 포함. 코드/기존 링크/URL·슬래시 없는 토큰·라인 없는 언급은 보수적으로 제외 ([#183](https://github.com/Swttch/swttch/issues/183), [#209](https://github.com/Swttch/swttch/issues/209), [#210](https://github.com/Swttch/swttch/pull/210))
- ["Open files with": detect and launch your editor of choice](./014-open_files_with/en.md) — 채팅에서 클릭한 파일 참조를 사용자가 고른 에디터/IDE(JetBrains 제품군·VS Code·Cursor·Xcode·Zed 등)로 여는 설정. GitHub Desktop 방식을 참고해 macOS 번들 ID(Spotlight 꺼져도 동작)·Windows 레지스트리·Linux PATH로 감지하고, 시스템 기본·직접 지정(경로+`%TARGET_PATH%` 인자)을 지원. 설정을 CLI 탭으로 이동, 터미널 감지도 같은 엔진으로 통합 리팩토링, 설정 저장을 원자화 ([#213](https://github.com/Swttch/swttch/issues/213), [#214](https://github.com/Swttch/swttch/pull/214))
- [@ file mentions find submodule files, and honor the fileSuggestion setting](./015-file_mention_submodules_and_filesuggestion/en.md) — `@` 파일 멘션이 git 서브모듈 내부 파일을 찾고(내장 인덱스를 `git ls-files --recurse-submodules`와 `--others` 2회 병합으로 개선, `.gitignore` 존중 유지), CLI의 `fileSuggestion` 설정을 존중해 `@` 인덱스 생성 명령을 사용자가 지정할 수 있게 함(설정 → General, `settings.json`과 동일 값·query를 stdin JSON으로 전달·실패 시 내장 인덱스로 폴백) ([#201](https://github.com/Swttch/swttch/issues/201), [#223](https://github.com/Swttch/swttch/pull/223))
- [@ file mentions rank with fuzzy subsequence matching (IntelliJ-style)](./016-mention_fuzzy_search/en.md) — `@` 파일 멘션 목록을 연속 부분 문자열 필터에서 IntelliJ Search Everywhere / CamelHump 방식의 퍼지(부분수열) 랭킹으로 교체해 `@bcva`가 `business-case-validation-architecture.md`를 찾게 하고(단어 경계·연속·basename 보너스로 순위, 흩어진 매칭 감점), 매칭된 글자를 드롭다운에서 강조(백엔드가 `matchIndices`를 반환→웹뷰가 원본 그대로 전달→`renderHighlightedPath`가 강조) ([#170](https://github.com/Swttch/swttch/issues/170), [#224](https://github.com/Swttch/swttch/pull/224))
- [Settings Badges](./017-settings_badges/en.md) — 설정 화면의 각 항목 옆에 작은 뱃지를 달아 그 설정의 "종류"를 한눈에 보여주는 기능. 파란 **S**(Sponsor) 뱃지는 후원자를 위한 기능임을, 주황 **C**(Claude Code native) 뱃지는 실제 Claude Code CLI 설정임을 표시하고, 뱃지에 호버하면 툴팁(그리고 C 뱃지는 공식 문서로 가는 링크)을 보여준다 (`feat/auto-resume-on-limit-reset` 브랜치)
- [Scheduled Messages](./018-scheduled_messages/en.md) — 채팅 메시지를 나중에 보내도록 예약하는 Slack식 "send later" 기능(후원자를 위한 기능). 슬래시 패널 → Context → "Schedule a message…"로 예약 팝오버를 열고(컴포저 초안 자동 채움, 메인 컴포저와 같은 리치 에디터), 프리셋·기간 지정·정확한 시각으로 발송 시각을 고른다. 발송 시각이 되면 사람이 직접 보낸 것과 동일한 경로로 한 탭에서 전송하며, 상단바 시계 버튼→패널에서 세션별 예약을 실시간 카운트다운과 함께 조회·수정·취소(CRUD)한다 (`feat/auto-resume-on-limit-reset` 브랜치)
- [Auto-resume on usage-limit reset](./019-auto_resume_on_limit/en.md) — 기존 예약 메시지 엔진으로 한도 리셋 후 대화를 재개하는 후원 기능. 계정 풀에서 선택한 계정에 예약을 연결하고, 충전 확인·새로고침·취소를 처리한다.
- [Models you connect yourself now show by their own name](./021-custom_model_catalog_display/en.md) — Opus/Sonnet/Haiku 자리에 직접 연결한 모델(`ANTHROPIC_DEFAULT_*_MODEL`)을 쓸 때, 대화를 시작하면 입력창 아래 표시와 모델 선택 패널이 **Default** 로 되돌아가고 그 자리에 들어간 긴 문장이 줄을 두 줄로 무너뜨리던 문제를 수정. 근본 원인은 이름을 못 알아본 것이 아니라 **"못 알아봤다"를 "기본값이다"로 단정**해 사용자 선택을 버린 것 — 이제 식별에 실패하면 폴백하지 않고 **기존 선택을 그대로 유지**한다(다음 요청도 그 모델로 나감). 이름 형태에 의존하지 않는 규칙이라 GLM 외 임의 커스텀 모델에도 대응. 부수적으로 식별 성공률도 높임(설명의 `Custom Haiku model` 활용, 접미사·대소문자 흡수)하고, 끝내 모르는 모델은 Default 인 척하지 않고 원본 값 그대로 표시. 입력창 아래 줄은 좁은 폭에서도 한 줄 유지 + 말줄임(권한·사용량 표시 포함) ([#217](https://github.com/Swttch/swttch/issues/217), [#202](https://github.com/Swttch/swttch/issues/202))
- [@ file mentions now work inside slash commands](./020-mention_inside_slash_command/en.md) — 슬래시 명령을 입력하는 중에 `@` 를 쳐도 파일 선택 목록이 뜨지 않던 문제를 수정. 슬래시 패널과 파일 목록이 입력창 위 같은 자리를 공유하는데 판단 기준이 "줄이 `/` 로 시작하나"뿐이라, 검색이 끝난 목록이 패널에 계속 가려져 있었다(바깥을 클릭하면 잠깐 보였다가 다음 글자에 다시 가려짐). 기준을 "커서가 `@토큰` 안에 있나"로 바꿔 두 기능이 자리를 나눠 쓰게 했고, 파일 선택 후 슬래시 패널 복귀·커서 되돌림 시 목록 재표시·이메일 주소 오작동 방지를 함께 정리. 스킬 전용 문제가 아니라 모든 슬래시 명령에 해당 ([#236](https://github.com/Swttch/swttch/issues/236))

- [Typing `/` mid-sentence opens the command panel](./022-slash_panel_after_text/en.md) — 문장을 쓰던 중에 `/` 를 치면 명령 패널이 아예 뜨지 않아 스킬·CLI 명령을 꺼낼 수 없던 문제를 수정. 패널이 "입력창 전체가 `/` 로 시작하나" 하나로만 열릴지를 판단했기 때문으로, 기준을 `@` 멘션과 같은 모양(줄 맨 앞이나 공백 뒤의 `/` + 커서가 그 토큰 안)으로 맞췄다(토큰 안에 `/` 가 또 있으면 경로로 보고 무시). 같은 기준에서 비롯된 두 번째 증상도 함께 정리 — 쓰던 문장 뒤에서 항목을 고르면 실행·전송하지 않고 이름만 채워 넣어(커서는 이름 뒤 공백 다음) 앞서 쓴 문장이 사라지지 않게 했고, 빈 입력창에서 고르면 지금까지처럼 즉시 실행 ([#244](https://github.com/Swttch/swttch/issues/244))
- [The view stays where you put it while you read](./023-auto_scroll_stays_put/en.md) — 답변이 흘러나오는 동안 위로 올려 읽고 있어도 화면이 자꾸 바닥으로 끌려 내려가던 문제를 수정. 원인은 두 가지였다. 첫째, "사용자가 새 메시지를 보냈나"를 대화 기록의 종류만으로 판단했는데 **도구 실행 결과도 사용자 메시지와 같은 종류로 기록**되기 때문에 파일을 읽거나 명령을 실행할 때마다 아래 따라가기가 다시 켜졌다(실제 대화 40개·26,941건 기준 384번 보냈는데 5,031번 끌려 내려감 — 92%가 불필요). 이제 사람이 직접 입력한 것(글·붙여넣은 이미지)만 전송으로 세고, 도구 결과·압축 요약 등 CLI가 스스로 기록한 항목은 제외한다. 둘째, **자동 스크롤 재개 거리** 설정이 "다시 따라가기" 판단에만 쓰이고 "멈추기"는 설정과 무관한 2픽셀 기준이 따로 쥐고 있어, 살짝만 올려도 자동 스크롤은 꺼졌는데 "맨 아래로 스크롤" 버튼은 안 뜨는 구간(기본값에서 0~80px)이 있었다. 이제 설정값 하나가 멈춤·재개 양쪽의 유일한 경계이며, 자동 스크롤이 꺼진 상태와 버튼이 보이는 상태가 항상 일치한다. 옛 동작을 적고 있던 설정 설명 문구도 12개 언어 전부 수정 ([#206](https://github.com/Swttch/swttch/issues/206))
- [UI Zoom](./024-ui_zoom/en.md) — `Cmd`/`Ctrl` + `+`/`-`/`0`, 또는 수식 키를 누른 채 스크롤로 GUI 전체(텍스트·아이콘·여백·테두리)를 확대·축소하는 기능. 브라우저와 같은 배율 단계(50%~300%)를 밟아 축소 후 재확대 시 정확히 원래 값으로 복귀하고, 조절 중엔 크롬 스타일 표시창이 우측 상단에 떠 실시간 퍼센트와 `−`/`+`/초기화 버튼을 제공(호버 시 유지, 반복 조작해도 팝업이 쌓이지 않고 숫자만 갱신). 기존 글꼴 크기 설정과는 독립적으로 곱해져 적용되며, 설정값은 자동 저장되어 다음 실행에도 유지 ([#169](https://github.com/Swttch/swttch/issues/169))
- [Collapsible prompt panels & clickable "Esc to cancel"](./025-collapsible_prompt_panels/en.md) — Claude가 되묻는 세 패널(도구 권한 확인·계획 승인·선택지 질문)을 우측 상단 버튼으로 한 줄 바까지 접어두고 다시 펼치는 기능. 좁은 화면에서 판단 근거가 될 대화 내용이 패널에 가려지던 문제를 해소한다 — 접기는 취소가 아니라 대기 유지이며, 접힌 바에 패널 제목이 남아 무엇이 대기 중인지 알 수 있고, 접은 동안에는 숫자 키 선택이 차단되어(Esc 취소는 유지) 뒤 내용을 읽다 실수로 승인되지 않는다. 접힘은 그 패널 하나에만 적용되어 다음 질문은 항상 펼쳐진 채 뜬다. 더불어 예전부터 안내문일 뿐이던 "취소하려면 Esc" 문구 자체를 클릭하면 Esc와 동일하게 취소되도록 해, 키보드가 없는 모바일·태블릿 접속에서도 안내대로 취소할 수 있게 했다 ([#248](https://github.com/Swttch/swttch/pull/248))
- [Pick which header icons stay out, and in what order](./026-customizable_header_dock/en.md) — 채팅 상단바 오른쪽에 기능마다 하나씩 쌓이던 아이콘 여섯 개를 더보기(⋮) 메뉴 하나로 정리하고, 자주 쓰는 것만 골라 ⋮ 왼쪽 "도크"로 꺼내 쓰게 한 기능. 아이콘이 늘수록 같은 폭을 나눠 쓰는 세션 제목이 먼저 잘려나갔고, 여섯 개는 사람마다 쓰임새가 갈리는데 모두에게 똑같이 자리를 차지했다. 기본값은 ⋮ 하나이며, 열면 편집 모드 없이 여섯 항목이 한 목록으로 보인다(Notion식 눈 아이콘 토글). 각 행은 손잡이(드래그)·눈(도크 노출)·행 자체(실행) 세 자리로 갈려 제스처가 겹치지 않고, 도크 아이콘 순서도 이 목록을 따른다. 배치는 전역 저장되어 프로젝트를 옮겨도 같은 자리에 있다. 도크에 꺼낸 기능도 메뉴에 그대로 남으며, 각 행 우측엔 도크 아이콘과 동일한 실시간 상태가 뜬다. 계정 전환은 고르는 목록이라 대상에서 빠져 ⋮ 오른쪽 상시 아이콘으로 남는다 (자체 기획, [#251](https://github.com/Swttch/swttch/pull/251))
- [`/reload-plugins` and `/btw` work in the GUI](./028-cli_only_slash_commands/en.md) — 터미널에서는 되는데 GUI에서는 `/reload-plugins isn't available in this environment.` 로 거절당하고 명령 목록에도 뜨지 않던 문제를 수정. 우리가 걸러낸 것이 아니라 **CLI가 보내주지 않은 것** — GUI는 CLI를 비대화형 세션으로 실행하는데, CLI는 일부 내장 명령을 터미널 전용으로 표시해 명령 목록에서 빼고 텍스트로 보내도 거절한다(같은 이유로 `/btw` 도 없었다). CLI가 같은 일을 하는 요청은 직접 받아주므로, 이미 CLI를 제어하는 데 쓰던 통로로 그 요청을 보내 터미널과 같은 동작을 하게 했다(전달 실패 시 평문 전송으로 폴백해 CLI의 설명이라도 받게 함). `/reload-plugins` 는 활성 플러그인·실패 개수를 보고하고 명령 목록을 새로 읽어 새 명령·에이전트를 곧바로 쓸 수 있게 하며, `/btw` 는 대화를 건드리지 않고 곁가지 질문에 답한다. CLI가 원래 제공하는 `/context`(사용량 카드)·`/usage`(계정 모달)는 손대지 않음 ([#270](https://github.com/Swttch/swttch/issues/270))
- [Empty bubbles are gone, and sentences containing `$` stay intact](./027-empty_bubbles_and_readable_logs/en.md) — 대화 중간에 테두리만 있고 속은 빈 말풍선이 줄줄이 뜨던 문제를 수정. 말풍선 상자가 내용물과 무관하게 테두리·배경을 먼저 그려, 표시할 글자가 없는 메시지(내부 안내표 `<system-reminder>`만 담긴 것, 열어둔 파일 정보만 담겨 본문이 비는 것 등)가 좌우 여백만 남은 18×9px 조각으로 화면에 남았다. 이제 글자·이미지·파일 칩 중 하나도 없으면 그리지 않는다(어시스턴트 답변 쪽이 원래 쓰던 기준과 통일). 단 **도구 실행 결과는 본문이 비어도 남긴다** — 위쪽 도구 카드로 합쳐지느라 비어 보이는 것이라, 아직 안 불러온 이전 대화에 그 카드가 있으면 지울 경우 도구 출력이 소리 없이 사라지기 때문. 조사 중 발견한 두 가지를 함께 수정: ① `$` 두 개 사이를 수식으로 착각해 셸 변수(`$VAR`)·가격이 든 평범한 문장에서 기호가 사라지고 **주변 문장이 여러 번 겹쳐 출력**되던 문제(`$…$`를 수식에서 제외, `$$…$$`·`\(…\)`는 유지) ② 버그 제보용 "Copy plugin front log"가 5,000줄만 담아 실제로는 80초분밖에 안 남던 문제(32MB 용량 기준으로 변경) — 제보자가 지시대로 보낸 로그가 정확히 상한에 잘려 원인 부분이 이미 밀려나 있었고 그 절반이 ①의 경고로 채워져 있었다 ([#232](https://github.com/Swttch/swttch/issues/232))
- [The chat now uses your IDE theme's own colors](./028-ide_theme_sync/en.md) — 채팅 화면이 다크 모드에서 `#1A1A1A`를 고정으로 칠해, IntelliJ Dark의 에디터 배경(`#1E1F22`)과 미묘하게 어긋나 경계에서 색이 튀던 문제. **테마를 열거하지 않는 방식**으로 해결했다 — JetBrains 테마는 기본 포함분·마켓플레이스·사용자가 직접 쓴 `.theme.json`까지 있어 개수를 셀 수 없으므로, 이름을 아는 코드를 두지 않고 지금 적용된 테마가 해석한 색을 런타임에 읽어 CSS 변수로 주입하고 테마가 바뀌면 다시 읽는다(그래서 어떤 테마든 코드 수정 없이 동작). 배경뿐 아니라 글자·테두리·강조·입력창·툴팁 색까지 받아오며, 테마가 정의하지 않은 항목만 자체 값으로 폴백한다. 컴포넌트 코드는 한 줄도 바뀌지 않았다 — 기존 시맨틱 토큰이 가리키는 값만 바꾸는 오버레이 레이어라, 앞으로 만들 컴포넌트도 토큰만 쓰면 동기화가 따라온다. 설정 → Appearance의 Color Theme 드롭다운 우측 "Sync with IDE" 체크박스로 켜며 **기본은 꺼짐**(켜면 드롭다운이 "Managed by IDE" 표시로 바뀌고, 끄면 원래대로 복귀). 테마 이름을 표시하지 않는 이유는 이름 조회에 필요한 `UIThemeLookAndFeelInfo`가 `@ApiStatus.Experimental`이라 마켓 zero-warnings 게이트와 충돌하기 때문(사용한 API는 전부 2024.2 바이트코드로 stable 검증). 호버 색은 IDE의 `List.hoverBackground`가 다크 테마에서도 `#edf3ff`(거의 흰색)이라 그대로 쓰면 행이 하얗게 튀고, 자체 고정값은 IDE 패널색과 채널당 ~3 차이라 구분이 안 돼 — IDE 패널색에서 밝기를 일정량 옮겨 파생시킨다 ([#267](https://github.com/Swttch/swttch/issues/267))
- [Speak into the chat instead of typing it](./029-voice_to_text/en.md) — 컴포저의 마이크 버튼이나 단축키(기본 `⌥D`)로 말하면 그대로 받아쓰기가 되는 음성 입력. 브라우저 내장 음성 API가 JetBrains WebView에 없어서 막혔고, 다른 Claude Code 클라이언트들이 쓰는 Anthropic 비공개 엔드포인트는 **자격증명 전송을 요구**해 플러그인이 다룰 수 없었다 — 그래서 그 부분만 [@swttch/extend-kit](https://www.npmjs.com/package/@swttch/extend-kit)(사용자 머신에 설치되는 패키지, 사용량 패널의 `ccb`와 같은 방식)으로 빼내 플러그인은 자격증명 코드를 한 줄도 담지 않는다. 이미 로그인된 계정으로 인증하므로 별도 API 키·요금이 없다. 받아쓴 텍스트는 **입력창 안 커서 자리에** 들어가며, 아직 확정되지 않은 부분은 흐리게 표시되다 확정되면 보통 글자가 된다. 마이크 버튼과 단축키 모두 **짧게 누르면 계속 녹음, 누르고 있으면 그동안만 녹음**으로 같은 규칙을 쓴다. 말하는 언어는 Claude 공식 `language`를 1순위로 따르고 비어 있을 때만 우리 설정을 보며(VS Code 확장과 같은 폴백 사슬), 선택지는 Intl로 생성한 BCP-47 전체다. `/voice`로 끈 상태는 GUI에도 이어진다. kit이 없으면 설정 섹션이 잠기고 그 자리에서 설치할 수 있으며, 설치는 volta·pnpm·yarn 등 **그 머신을 관리하는 매니저**로 나간다 — 어느 매니저인지는 **터미널에서 쓰는 `claude`를 기준**으로 정하고(없으면 백엔드 Node로 폴백), npm으로 떨어질 땐 PATH가 아니라 **백엔드 Node 옆의 npm**을 쓴다(이 구분이 없으면 다른 Node의 전역 폴더에 설치가 성공하고도 로더가 못 찾는다). 설치된 뒤에는 버전 오른쪽 **휴지통 버튼**으로 같은 자리에서 지울 수 있고(평소 버전 글자와 같은 크기·색, 호버 시 빨강 / 확인 모달 → 토스트 → 버전 줄 새로고침), **버전 글자를 누르면 다시 확인**한다(터미널에서 밖으로 바뀐 경우를 잡으려는 것이므로 백엔드의 탐색 캐시까지 무시하고 처음부터 다시 찾는다). 조사 과정에서 **"어느 패키지 매니저냐"가 실은 세 축**(Node 버전을 관리하는 **런타임 매니저** / 전역 npm 패키지를 관리하는 **라이브러리 매니저** / `claude` 앱을 배포하는 **앱 채널**)이며 volta가 앞의 두 축에 동시에 속한다는 것이 드러나, 한 값으로 뭉쳐 있던 판정을 좌표로 분리했다 — **volta 자체 store와 "volta가 관리하는 Node의 npm 전역"은 서로 다른 곳**이라 전자만 지우면 후자가 남아 "삭제했는데 그대로"가 된다. 그래서 삭제는 알려진 **모든 store를 훑고, 성공 판정을 종료 코드가 아니라 디스크 재조회로** 한다. 덧붙여 npm은 `npm_config_prefix` 환경변수를 물려받으면 **엉뚱한 폴더를 상대로 `up to date`를 출력하며 정상 종료**하므로(실측), 모든 npm 명령에 `--prefix`로 대상을 못 박는다(단 사용자에게 붙여넣으라고 주는 안내 문구에서는 이 내부 경로를 제거한다) ([#235](https://github.com/Swttch/swttch/issues/235), [#298](https://github.com/Swttch/swttch/issues/298))
- [See the edit in your IDE — and keep only the parts you want](./030-ide_diff_review/en.md) — 파일 편집 승인이 "config.txt에 이 편집을 적용하시겠습니까?" 한 줄뿐이라 **무엇이 바뀌는지 모른 채 예/아니요를 눌러야 했고**, 열 군데 중 여덟 군데만 맞아도 전부 받거나 전부 버리는 수밖에 없던 문제를 해결. 이제 Claude가 파일 편집을 요청하면 **IDE의 좌우 비교 diff 창**(버전 관리에서 쓰던 그것)에 변경이 열리고, 변경이 여러 곳이면 그 창 안에 **곳마다 체크박스 + Apply/Reject**가 붙는다. 전부 체크된 상태로 시작하므로 그냥 Apply를 누르면 예전 승인과 동일하고, 체크를 풀면 바 왼쪽이 지금 몇 개를 받는지 말한다("1 of 2 selected"). 전부 풀면 Apply가 꺼진다 — 아무것도 안 받는 건 거절이고, 안 바뀐 파일을 쓰면 일어나지 않은 편집이 성공으로 보고되기 때문. **선택은 diff에서 한다** — 변경이 읽히는 자리에서 결정하는 것이고, 채팅 승인 화면은 손대지 않아 기존 조작이 그대로다. 체크를 푼 부분은 사후 되돌리기가 아니라 **애초에 쓰이지 않는다**(단 이 경우 Claude는 자기 제안을 기준으로 계속 이야기한다 — 편집까지 했을 때만 알림이 나가며, 040 참조). 구현 핵심은 CLI가 권한 응답의 `updatedInput`을 존중한다는 점(실측)이라 파일 쓰기를 가로채지 않고 **툴 호출 자체를 사용자가 고른 만큼으로 다시 적어** 넘긴다 — 단 Edit에 Write 모양 입력을 주면 CLI가 "File has not been read yet"로 거절하므로(이것도 실측) 답하는 툴의 모양을 유지한다. 선택은 hunk 번호만 오가고 파일 내용은 IDE를 왕복하지 않는다. IDE diff 창의 체크박스·버튼은 `DiffUserDataKeysEx.BOTTOM_PANEL`로 얹으며 Plugin Verifier 경고 0건. diff 창은 질문이 떠 있는 동안에도 닫힐 수 있어(diff에 포커스를 두고 Esc) 승인 화면 **제목의 파일 이름이 링크**다 — 누르면 그 편집의 diff가 다시 열리고(이미 열려 있으면 탭만 앞으로, 체크 유지), 보는 것은 답하는 것이 아니라 질문도 턴도 그대로다. IDE diff를 쓸 수 없는 곳에선 평범한 글자로 남는다. 조사 중 함께 고친 것 셋: ① "Esc to cancel" **클릭**이 아무 일도 안 하던 문제(React가 클릭 핸들러에 넘긴 MouseEvent가 거절 사유 자리에 앉아 직렬화가 터지고 메시지가 전송조차 안 됨 → CLI가 영영 대기) ② 취소가 이 요청만 거절하고 턴은 살려둬 Claude가 다음 도구로 넘어가 거절을 정리해 쓰던 문제(중단이 답변으로 돌아옴 — 다른 두 패널은 원래 턴도 멈춘다) ③ 마지막 탭이 닫히면 컴포저 파일 뱃지가 사라진 탭 이름을 계속 달고 있던 문제(탭 전환 리스너가 "남은 파일 없음"을 삼켜 이후 아무 푸시도 안 감). 설정 → IDE에서 끌 수 있고 끄면 이번 릴리즈 이전과 완전히 동일하며, IDE 밖(standalone)에서는 항목이 비활성이고 승인은 기존처럼 파일 단위. IDE 전용 설정 2개(에디터 파일 첨부·입력창 포커스)도 '일반'에서 새 **IDE** 섹션으로 이동 ([#109](https://github.com/Swttch/swttch/issues/109), [#41](https://github.com/Swttch/swttch/issues/41))
- [Voice input you will not use can now be switched off](./031-voice_first_use_prompt/en.md) — 음성 입력은 **켜진 채로 시작**하는데 실제로 쓰려면 `@swttch/extend-kit` 설치가 필요해서, 사내 정책 등으로 설치가 막힌 사용자는 **쓰지도 못하는 마이크 버튼을 없앨 방법이 없었다**("stuck with a voice icon forever"). 정작 끄는 스위치는 **원래부터 있었다** — 설정 → 일반 → 음성 입력의 토글. 다만 킷이 없으면 `SettingSection disabled` 가 섹션 전체를 `pointer-events-none` 으로 잠가 **설치하지 못하는 사람만 끄지도 못하는** 상태였다. 그래서 새 설정을 만들지 않고 **잠금 범위만 좁혔다**(토글은 예외, 킷이 있어야 의미 있는 사용 언어·대기 시간·단축키 3행은 그대로 잠김). 여기에 더해 **설정까지 찾아가지 않아도 되게** 첫 마이크 클릭에서 한 번 묻는다 — 버튼은 그대로 노출하되(존재를 알려 시도하게) 누르면 녹음 전에 무엇을 설치하는지 설명하고 고르게 하며, **"사용 안 함"은 그 토글을 끄는 것과 동일한 요청**을 전역 스코프로 보낸다(CLI `/voice` 와 같은 네이티브 키라 터미널 받아쓰기도 함께 꺼지는 것은 "한 머신 한 답" 원칙에 따른 의도된 동작). **"설치하고 사용"은 설치까지만 하고 녹음을 잇지 않는다** — 사용자가 요청한 적 없는 우회로였으므로 예상 못 한 채 마이크가 열리는 것보다 준비됐을 때 다시 누르는 편이 낫다(설치 실패도 "쓰겠다"는 의사를 되돌리지 않는다). 창은 **X·바깥 클릭·`Esc` 로 닫히지만 닫기는 응답이 아니다** — 미결로 남아 다음 클릭에서 다시 묻는다. 여기서 "사용 안 함"은 창을 접는 게 아니라 기능을 끄는 선택이라 무심코 누른 키 하나가 그 결과를 만들면 안 되기 때문이며, 이를 위해 `ConfirmDialog` 에 `onDismiss` 를 더해 **취소와 닫힘을 분리**하고 훅에 3값을 돌려주는 `ask()` 를 뒀다(기존 `confirm()` 호출부는 그대로 — 일반 확인창에선 닫기가 곧 취소가 맞다). 응답은 `profile.json` 에 `status`·`askedAt`·`decidedAt` 로 남기며 **질문 시각과 응답 시각을 분리**해 "묻긴 했는데 안 답했다"와 "아직 묻지도 않았다"를 구분한다. 킷이 이미 있으면 묻지 않고 바로 녹음한다(`askedAt` 은 null — 실제로 띄운 적이 없으므로). 컴포저의 "음성 입력에는 @swttch/extend-kit이 필요합니다" 안내줄+설치 버튼은 **유지한다** — 중복으로 보고 지웠다가 검수에서 뒤집혔다. **"설치하고 사용"으로 답한 뒤 킷을 지운 사용자**는 이미 답했으므로 질문이 다시 안 뜨고, 그 상태에서 마이크를 누르면 이 배너가 뜬다 — 설치 버튼이 없으면 **문제만 알리고 해결 수단은 없는 화면**이 된다 ([#299](https://github.com/Swttch/swttch/issues/299))
- [Several projects in one repository, reachable from one place](./032-nested_working_dirs/en.md) — 저장소 하나에 `packages/` 로 하위 프로젝트를 여러 개 두면 **두 단계 아래는 드롭다운에 아예 안 떴다**(직계 자식 한 단계만 잡던 필터). 깊이 제한을 없애고, 중간에 낀 폴더(`packages` 처럼 세션이 없는 경로 조각)는 **클릭 안 되는 스캐폴드 줄**로 복원해 트리 모양을 유지한다. 가지 글리프(`├─`)는 걷어냈다 — IntelliJ·VS Code 둘 다 elbow 커넥터를 안 쓰고 세로 가이드선을 쓰며(공식 문서로 확인), 우리 글리프는 세로줄이 없어 조각만 떠 있었다. 대신 **폴더 아이콘(채움=세션 있음 / 빈=스캐폴드·draft) + chevron + 들여쓰기**로 바꿨고, 들여쓰기는 Tailwind 클래스 팔레트가 런타임 값에서 조용히 clamp되므로(게다가 `ps-15`·`ps-18` 은 기본 스케일에 없어 **CSS가 아예 생성 안 됨** — 빌드 산출물 grep으로 확인) 인라인 `paddingInlineStart` 로 뺐다. 여기에 **"하위 프로젝트 포함" 토글**(설정 저장)로 하위 워킹디렉토리 세션까지 한 목록에 합친다. 이를 위해 **`rootDir`(어디서 보는가)과 `workingDir`(이 세션이 어디서 도는가)을 상시 함께 운영**한다 — 예외 분기가 아니라 정식 모델이고, 해석은 `WorkingDirContext` 한 곳에서만 하며 파라미터 부재는 "둘이 일치"를 뜻한다. URL은 앵커가 **항상 조상**이라는 성질을 이용해 절대경로 대신 `rootUp=2` 홉 카운트로 싣는다(269자→163자). 세션 행은 **프로젝트를 제목 위 별도 줄**에 두고(한 줄에 욱여넣으면 제목·배지가 둘 다 잘림), 프로젝트 이름도 제목·세션ID와 동등한 검색 대상이다. 상단 드롭다운과 좌측 세션 패널이 같은 `SessionList`·`useSessionList` 를 공유해 양쪽에 동시 적용된다
- [No more scrolling back to see what you asked for](./033-sticky_user_message/en.md) — 오래 걸리는 작업을 시켜놓으면 도구 호출·사고 과정이 길게 이어지면서 **정작 그것을 시작시킨 내 지시가 화면 밖으로 밀려나**, 지금 벌어지는 일이 요청한 것과 맞는지 확인하려면 위로 한참 올라갔다 내려와야 했다. 이제 사용자 메시지는 **그에 대한 답변이 화면에 남아 있는 동안 상단에 고정**되고, 다음 메시지를 보내면 새 지시가 그 자리를 넘겨받으며 이전 것은 위로 밀려난다. 제보자가 제안한 **색 강조는 넣지 않았다** — 두 방법 모두 "스크롤하다 지시를 발견하기 어렵다"는 같은 문제를 겨냥하는데 고정이 그 문제를 한 단계 앞에서 없애므로, 이미 늘 보이는 것을 눈에 띄게 만들 이유가 없다. 구현은 CSS가 아니라 **화면 구조 변경** — 고정은 자기가 속한 영역 밖으로 못 나가므로, 지시 하나와 딸린 답변들을 한 묶음으로 만들어야 묶음이 빠져나갈 때 고정도 끌려 나가고 다음 지시가 자리를 넘겨받는다(평평한 형제 목록이면 같은 자리에 겹쳐 쌓여 나중 것이 앞의 것을 덮는다). **도구 실행 결과도 사용자 메시지와 같은 종류로 기록**되므로(실제 대화에서 사람이 보낸 것보다 열 배쯤 많음) 종류만 보고 고정했다면 도구가 돌 때마다 빈 칸이 상단에 박혔을 것 — [#206](https://github.com/Swttch/swttch/issues/206)에서 같은 함정으로 화면이 바닥에 끌려가던 것을 고치며 만든 판별 규칙을 새로 만들지 않고 재사용했다. 고정된 지시 위로 내용이 비쳐 보이던 틈은 **상단바 실측 34px과 대화 영역이 비워둔 40px의 6px 어긋남**이었고, 상단바 높이를 못 박아 해결(두 값은 짝이라 한쪽만 바뀌면 재발하므로 양쪽에 주석). 아래로 지나가는 대화는 불투명 배경으로 잘라내는 대신 **그라데이션으로 흐려지게** 해 이음매처럼 보이던 경계선을 없앴다. 더해서 고정된 지시에 **원래 자리로 돌아가는 버튼**을 달았다 — 고정은 "무엇을 시켰나"에 답하지만 "그게 대화의 어디쯤이었나"는 알려주지 않아, 화면 위의 사본에서 실제 위치로 가려면 여전히 스크롤로 찾아야 했기 때문. 버튼은 **실제로 걸려 있을 때만** 뜬다(제자리 지시에 달면 눌러봐야 그 자리인 버튼이 모든 지시마다 붙는다) + 지시에 호버할 때만 보인다(상시 노출은 화면에 지시가 여럿일 때 그 자체로 어수선). 걸린 상태 판정은 **스크롤 감시가 아니라 지시 위 높이 0 표식의 화면 이탈**로 한다 — 대화가 수천 건이라 지시마다 매 프레임 도는 핸들러는 이 뷰가 감당할 비용이 아니고, 브라우저는 상태가 바뀔 때만 알려준다. 착지 여백은 표식의 `scroll-mt-16` 한 숫자로 조절하되 **Tailwind 기본 스케일 밖 값은 CSS가 아예 생성 안 돼 여백이 조용히 0이 된다**(빌드 산출물 grep으로 확인) ([#274](https://github.com/Swttch/swttch/issues/274))
- [Every model in the list is reachable, however many there are](./034-model_list_scroll/en.md) — 선택 가능한 모델이 16개를 넘으면 **넘친 모델을 보지도 고르지도 못했다**. 모델 선택 패널은 입력창 위로 자라는데 **높이 상한도 스크롤도 없어서**, 카탈로그가 길면 화면 위쪽을 뚫고 자라고 밖으로 나간 행은 그릴 곳이 없어 사라진 채 도달할 방법이 없었다(목록에는 있는데 못 고르는 상태). 상한을 두고 그 안에서 목록이 스크롤되게 했으며, 상한은 고정 숫자가 아니라 **입력창 위에 실제로 남은 공간**으로 정한다 — 하나로 박으면 낮은 창에서 패널이 상단바 아래로 파고들어 자기 머리말을 스스로 가리기 때문(창 크기 변경 시 재측정). 값 자체는 새로 짓지 않고 **같은 자리에 뜨는 슬래시 명령 패널의 320px을 그대로** 가져왔다 — 두 패널의 코드는 위치·그림자·테두리·여백까지 거의 글자 단위로 같았고 딱 상한과 스크롤 두 줄만 모델 쪽에 빠져 있었다. 스크롤은 패널 전체가 아니라 **목록에만** 걸어 "모델 선택" 머리말이 함께 떠내려가지 않게 했고, 목록이 길 때 **현재 모델이 아래쪽이면 열자마자 그 자리로 미리 내려** 어떤 모델이 켜져 있는지(체크 표시)가 화면 밖에 머물지 않게 했다. 함정 셋: ① **상한만으로는 목록이 안 줄어든다** — 세로 배치에서 각 칸은 자기 내용물보다 작아지기를 기본적으로 거부하므로 줄어들어도 된다고 명시해야 상한 안으로 들어온다 ② 작은 창에서 머리말이 배너에 가리는 건 패널이 삐져나가서가 아니라 **배너가 위에 그려지는 것**이고(z-index는 같은 층에서만 비교되므로 숫자가 커도 층이 다르면 소용없음), 이번 제보와 별개이며 수정 전에도 같았으므로 **건드리지 않았다** ③ 처음 쓴 스크롤 테스트는 **계산된 스타일을 읽어 아무것도 재지 않고 통과**했다 — 이 프로젝트의 테스트 환경이 속도를 위해 스타일 파일을 비워두기 때문으로, 수정을 일부러 되돌려 실패를 확인하고서야 드러났다(지금은 코드에 붙은 값을 직접 확인) ([#314](https://github.com/Swttch/swttch/issues/314))
- [A pinned instruction folds away as you scroll](./035-pinned_send_scroll_fold/en.md) — 지시를 상단에 고정하고 나니 **긴 지시가 화면을 거의 다 덮어**, 정작 그 지시가 만든 답변을 보러 내려왔는데 절반이 내가 쓴 글이었다. 이제 고정된 지시는 **스크롤한 만큼 1:1로 접히고** 한 줄에서 멈추며, 되감으면 내려온 만큼 그대로 펴진다(아랫변이 원래 자리를 따라가 상자가 줄어드는 게 아니라 화면 위로 잠기는 것처럼 보이고, 원래 높이를 되찾는 순간이 곧 고정이 풀리는 순간이라 둘이 저절로 맞물린다). 하한은 한 줄보다 **몇 픽셀 크게** 둬서 둘째 줄이 살짝 걸쳐 보이게 했다 — 딱 한 줄만 남기면 **원래부터 한 줄이던 지시와 구분이 안 되기** 때문이고, 짧은 지시는 상한이 자기 높이라 이 값까지 늘어나지 않으므로 **둘의 차이가 곧 신호**가 된다. 본문 클릭 시 전부 펼침은 그대로 살아 있고(화면 80%까지, 넘치면 지시 안에서 스크롤 — 고정된 지시는 **페이지를 스크롤해도 그 안을 읽을 수 없어서** 이 방법뿐), 다시 접으면 펼칠 때 높이가 아니라 **지금 스크롤이 가리키는 높이**로 돌아간다(펼친 동안에도 계산은 계속 돈다). 함정 넷: ① 처음엔 높이를 **표식의 화면상 위치**로 쟀는데 접히면 그 표식이 움직여서(문서가 짧아짐 → 스크롤 보정 → 표식 이동) **접힘이 자기 출력을 입력으로 먹으며** 아랫변이 떨렸다 — 접힘이 건드릴 수 없는 유일한 값인 스크롤 위치만 보도록 바꾸고 레이아웃은 아예 안 읽는다 ② 고정된 요소는 떠 있어 보여도 **문서에서 자기 자리는 그대로 차지한다**(249px 접으니 문서도 정확히 249px 짧아짐 → 4px 스크롤에 화면은 8px 이동) — 접힌 만큼 채우는 자리가 필요한데 그걸 **고정된 요소 안에** 두면 빈 칸까지 화면에 붙어 177px 검은 띠가 생기므로(이 기능이 없애려던 바로 그 공간) **밖으로** 빼야 한다 ③ 시작 높이를 상자에서 그냥 읽으면 **이전 접힘 값이 남아 있어 접힌 값에서 또 접힌다** — 긴 지시는 최대높이 제한 덕에 늘 같은 값이 나와 멀쩡했고 **한 줄짜리만 깜빡였다**(접힘을 걷어내고 그 아래 원래 높이를 읽어 해결) ④ 하한만 있고 **상한이 없어** 되감아 원점 위로 가면 한 줄짜리가 448px 빈 상자로 그려졌다(이것도 긴 지시는 캡이 가려줌) — 다만 계산값 자체는 위아래로 넘치게 두어야 한다, 500px 지나쳐 접고 10px 올렸을 때 바로 펴지면 안 되고 **되짚어 올라온 만큼만** 펴져야 하므로 **자르는 것은 그리는 순간에만** 한다. 매 프레임 도는 계산이라 감시자는 지시마다가 아니라 **스크롤 영역에 하나**만 두고 지금 고정된 하나만 계산하며(프레임당 1회로 병합), 그 값은 중간 컴포넌트를 통과시키지 않는다 — memo된 그 컴포넌트로 매 프레임 값을 흘리면 **그 값을 쓰지도 않는 모든 종류의 메시지가 매 프레임 다시 그려진다**
- [Long lines fold so you can read them at a glance](./036-soft_wrap/en.md) — 고정폭 블록(Edit diff 카드·도구 `IN`/`OUT`·AskUserQuestion 기록 카드)이 줄을 접지 않아 **긴 줄이 카드 폭에서 잘리고 가로 스크롤로만 도달**됐다. diff에서는 지운 줄/넣은 줄이 붙어 있는데 정작 달라진 부분이 잘린 뒤쪽이면 무엇이 바뀐지 알 수 없었다. `설정 > 모양`에 **자동 줄바꿈**(`softWrap`, 기본 off) 토글을 넣고 `pre-wrap`+`break-all`로 접는다(경로·JSON은 공백이 없어 단어 경계만으로는 여전히 넘침). 값을 각 컴포넌트가 읽지 않고 **`<html>`에 `.soft-wrap` 클래스 하나만 토글**해 CSS가 보게 했다 — 대상 블록이 열 곳 넘게 흩어져 있어 구독·prop을 그만큼 늘리지 않으려는 것(대신 연결이 끊겨도 화면은 멀쩡해 보이므로 클래스 토글 자체를 테스트로 고정). 함께 고친 두 번째 제보가 **배경색이 텍스트 끝까지 안 감**인데, 이건 **랩과 무관하게 항상 적용**된다(기본값이 off라 끈 상태에서도 맞아야 함). 원인은 diff 행이 가로 스크롤 `<pre>` 안의 블록이라 **폭이 부모의 콘텐츠 박스=보이는 폭으로 풀리는 것**. 함정: ① 행마다 `width:fit-content`를 주는 건 **오답** — 각 행이 자기 텍스트 길이만 가져 한 카드에서 1227~4704px로 제각각이 되고 행마다 다른 지점에서 tint가 끊긴다(고쳤다고 보고했다가 사용자가 화면에서 잡아냄) ② 행에 `min-width:max-content`도 안 됨 — 블록 자식은 부모 콘텐츠 박스 기준이라 보이는 폭으로 풀린다. 정답은 `<pre>` 안에 **`min-width:max-content` 래퍼(`.diff-body`)를 두고 행이 `min-width:100%`로 그것을 채우는 것**(래퍼는 `.soft-wrap`에서 `min-width:0`으로 되돌려야 접힘) ③ 검증에서 **프로브 한 줄만 재면 이 결함은 관찰 자체가 불가** — 값이 아니라 `new Set(widths).size === 1` 같은 **행들 사이의 관계**를 단언해야 한다. jsdom은 레이아웃을 안 하므로 CSS 원문(`?raw`)을 읽어 `fit-content` 재유입을 막는 테스트를 함께 둠. 부수 정리로 `모양` 첫 섹션의 "테마" 제목 제거(`SettingSection`의 `title`을 옵셔널화 — 화면 이름이 이미 "모양"이라 바로 아래 "테마"는 한 줄을 쓰며 구분을 못 함) ([#179](https://github.com/Swttch/swttch/issues/179))
- [When installing the voice input kit said "done" but nothing worked](./037-confirm_extend_kit_install/en.md) — 음성 입력 키트 설치가 **성공했다고 표시되는데 실제로는 안 되던** 문제. 완료 판정을 **명령의 종료 코드**로 하고 있었는데, 종료 코드가 답하는 것은 "명령이 잘 돌았나"이지 "이제 음성 입력을 쓸 수 있나"가 아니다. 전역 npm 패키지가 어디 깔리는지에 정답이 하나가 아니라(volta는 자기 저장소, pnpm·yarn은 각자의 전역 폴더, npm은 활성 Node의 `lib/node_modules`, 여기에 `npm_config_prefix`가 끼면 우리가 고른 npm조차 딴 데 쓴다) **설치는 A에 하고 프로그램은 B를 보면 명령은 0을 반환하고 기능은 안 된다** — #298의 모든 사연이 이 한 모양이다. 이제 **패키지를 실제로 찾았는지**로 판정하며, 찾을 때 음성 입력이 키트를 불러올 때 쓰는 **바로 그 해석 경로**를 그대로 쓴다 — "설치한 곳"과 "불러오는 곳"을 따로 확인해 비교하는 대신 **하나의 질문(지금 불러올 수 있나)으로 합치는** 것이라, 위 매니저들을 경우별로 특수 처리하지 않고 한 번에 덮는다(캐시는 조회 전에 비운다 — 설치 이전의 "못 찾음"이 그대로 답으로 나오기 때문). 처음에 못 찾으면 **설치를 한 번 더 실행**한다: 제보자의 우회법이 정확히 같은 명령의 재실행이었고, 이미 깔린 패키지에 `npm i -g`는 아무 일도 안 하므로 안전하다. 단 **명령 자체가 실패한 경우는 재시도하지 않는다** — 재실행이 해결할 게 없고 진짜 오류를 보기까지 대기만 두 배가 된다. 두 번 다 못 찾으면 실패라고 단정하지도(실제로 실패한 게 아니다) 내부 사정을 설명하지도 않고 **"설치가 정상적으로 마무리되지 않았습니다. 다시 한 번 시도해주세요"**만 말한다. 확인된 뒤 화면은 **버전 표시 기능에 넘긴다**(버전 숫자를 눌렀을 때 도는 그 조회) — 설치 쪽에 "설치됨"을 따로 기억해두면 두 판단이 언젠가 어긋나기 때문. 함께 고친 것: 권한이 필요한 위치(`/usr/local` 등)에서 명령이 실패할 때 건네던 **복사해 쓸 수 있는 sudo 명령 안내가 4초 만에 사라지고 있었다**(알림 기본값) — 여러 줄을 읽고 명령을 찾아 터미널에 옮겨 적기엔 턱없이 부족해 **제보자는 이 안내를 못 보고 직접 명령을 알아냈다**. 이제 설치 실패 안내는 **직접 닫기 전까지 사라지지 않고**(닫기 버튼 추가, 줄바꿈 보존), "설치가 마무리되지 않았습니다" 안내도 같은 길로 전달된다 — 다시 눌러보라고 알려주는 유일한 통로이므로 ([#298](https://github.com/Swttch/swttch/issues/298))
- [So filing a bug report no longer means digging through IDE menus](./038-about_ide_os_version/en.md) — 정보 화면에 **IDE/브라우저**와 **OS** 두 줄을 추가. 제보의 요지는 "버그 리포트 양식이 IDE 버전과 OS를 묻는데, 그걸 알아내려면 메뉴를 한참 타야 한다 — 플러그인은 자기가 어디서 도는지 이미 알잖나"였다. 실제로 알고 있었다: Kotlin이 `IntelliJ IDEA 2024.2 (IU-242.20224.300)`(제품+버전+**빌드번호**)를 만들어 `CCG_CLIENT_INFO`로 백엔드에 넘기고 있었고 **텔레메트리만 읽고 화면은 아무도 안 쓰고 있었다**. 브라우저 접속이면 같은 자리가 UA 원문이라 옮겨 적기엔 너무 길어 `bowser`(의존성 0, MIT)로 `Chrome 149.0.7827.55`까지만 줄인다 — 직접 파싱하지 않은 이유는 **Edge의 UA에는 `Chrome`이 들어 있고 Safari의 UA에는 `Chrome`이 없어서** 눈으로 짠 매칭이 Edge를 Chrome이라 부르기 때문. 손이 많이 간 쪽은 OS다: `os.release()`는 **커널**을 답해서 macOS에선 실제 `26.5.2`인 기기가 `25.5.0`으로 나오고(그대로 보여줬으면 제보자가 틀린 번호를 적었을 것), Linux에선 **배포판**을 아예 말하지 않는다 — 제보자가 "Linux에선 OS가 중요하다"고 한 게 정확히 그 배포판이다. 그래서 OS마다 그것을 실제로 식별하는 곳에서 읽는다(macOS `sw_vers`, Linux `/etc/os-release`, Windows `os.release()`), 어느 경로든 실패하면 커널 정보로 물러난다. **브라우저에게 OS를 묻지 않는다** — 애플이 UA의 macOS를 `10_15_7`로 고정해둬서 `bowser`가 26.5.2 기기를 두고 `Catalina`라고 이름까지 붙인다(틀린 값을 자신 있게 말하는 게 아무 말 안 하는 것보다 나쁘다). 백엔드는 사용자 기기에서 직접 도니 그냥 더 정확히 안다. 남긴 한계: Windows의 `os.version()`은 문서에 "커널 버전"이라고만 적혀 있고 개발 환경이 macOS라 모양을 확인 못 해, **이름처럼 읽힐 때만 덧붙이고 빌드번호를 대신하지는 않게** 했다 ([#320](https://github.com/Swttch/swttch/issues/320))
- [Developer tools moved off F12 and into settings](./039-devtools_off_f12/en.md) — 채팅 패널을 한 번 열고 나면 `Alt+F12`가 IDE 터미널(WebStorm 기본 단축키) 대신 개발자 도구 창을 열던 문제. 키맵에도 없고 레지스트리 설정(`ide.browser.jcef.contextMenu.devTools.enabled`)을 꺼도 그대로였는데, 채팅을 그리는 내장 브라우저의 키 처리기가 **IntelliJ 단축키 체계보다 아래에서** 키를 먼저 받아 F12를 가로채기 때문이라 IDE 설정으로는 되돌릴 수단이 없었다. 처음엔 "수식키가 붙은 F12만 IDE로 넘기자"고 조건을 다듬으려 했으나, 실제로 키를 눌러 재보니 **F12만 눌러도 개발자 도구와 터미널이 동시에 열려** 그 방향이 성립하지 않았다 — F12는 IDE가 이미 쓰는 키라 우리가 무엇을 걸든 남의 단축키와 겹친다. 그래서 조건을 손보는 대신 **키를 아예 쓰지 않기로** 하고, 설정 → IDE 연동의 "개발자 도구" 항목에 **열기** 버튼을 두어 그곳을 유일한 진입로로 삼았다(브라우저 접속 시에는 자체 개발자 도구가 있으므로 비활성). ([#333](https://github.com/Swttch/swttch/issues/333))
- [Changing a global setting did nothing, and looked like it worked](./040-project_override_lock/en.md) — 프로젝트 설정이 이미 잡고 있는 항목을 전역에서 바꾸면 화면은 바뀐 것처럼 보이는데 새로고침하면 되돌아가던 문제. 설정은 전역 → 프로젝트 순으로 겹쳐지므로 그 저장은 애초에 유효값을 바꾸지 못하는데, 웹뷰가 저장 결과를 기다리지 않고 미리 그리는 낙관적 업데이트를 하면서 **그 값이 실제로 이기는지는 확인하지 않았다**. 백엔드는 이미 답을 주고 있었다 — 병합 설정을 읽을 때마다 프로젝트가 가져간 키 목록(`overrides`)이 함께 오는데 웹뷰가 쓰지 않고 있었을 뿐이라, 상태를 새로 만들지 않고 캐시에 이미 있는 그 목록만 보고 판정한다. 처음엔 배지로 알리기만 하려 했으나 **알려주고도 바꿀 수 있으면 같은 혼란이 남아**, 전역 탭에서 해당 항목을 비활성화해 애초에 바꿀 수 없게 했다(프로젝트 탭은 그대로 — 거기서 보는 값이 이기는 값이다). 곁들여, 프로젝트 탭의 `설정 안 함`이 키를 지우는 대신 `null`을 적어두고 있어 그 키가 계속 override로 잡히고 전역값을 `null`로 덮던 것도 고쳤다. ([#344](https://github.com/Swttch/swttch/issues/344))
- [Background tasks can actually be stopped now](./041-cancel_background_tasks/en.md) — Background tasks 패널의 `✕`가 **목록에서 감추기만 하고 작업은 계속 돌고 있던** 문제. 코드를 열어보니 `✕`는 화면 목록에서 id 하나를 빼는 게 전부였고 백엔드로 나가는 요청이 없었다. `Escape`도 같은 모양이었는데 이쪽은 반대 방향으로 어긋나 있었다 — CLI에 interrupt를 보내면서 **동시에 그 세션의 워크플로우를 전부 `stopped`로 칠하고 있었고**, 칠하기만 한 것이라 작업은 그대로 돌았다(화면은 죽었다고, 실제로는 살아서). 실측으로 확정했다: `interrupt`를 보내면 CLI는 턴을 끝내고 `result`를 내지만 백그라운드 작업에는 `task_notification`이 가지 않고 계속 돈다(대조군 45초간 `task_started`만, 실험군은 보낸 즉시 `stopped`). 그래서 `stopWorkflowsForSession` 호출을 걷어냈다 — 이제 `running`으로 남아 있으면 정말 도는 것이다. 취소는 **공식 CLI가 받는 `control_request{subtype:'stop_task', task_id}`**로 보낸다. 이 subtype은 미지원 subtype이 `Unsupported control request subtype: X`를 돌려주는 것을 판별기 삼아 후보 11개를 태워 찾았고(나머지는 전부 unsupported), **인자 없이도 success가 오므로 success 자체는 증거가 못 돼** 실제로 도는 작업으로 대조 실험을 해서 확정했다. `task_id`는 `task_started` 이벤트가 `tool_use_id`와 **함께** 실어주므로 새로 만들 상태가 없다. 폴백은 **`<system-reminder>`로 감싼 유저 메시지**로 모델에게 `TaskStop`을 요청하는 것 — `parseUserContent`가 그 태그를 걷어내고 남은 텍스트가 비면 `UserMessageRenderer`가 아무것도 그리지 않아 (#232에서 이미 다뤄진 경로) 대화에 뜨지 않는다. 폴백을 남긴 이유는 멀티 프로바이더다 — `stop_task`는 Claude CLI의 것이라 다른 CLI로 안 옮겨가지만, "모델이 있고 멈추는 도구가 있다"는 어디서나 성립한다. 한 번에 다 멈추는 수단으로 **`Escape` 3연타 + 확인창**을 뒀는데, 세는 기준이 **인터럽트 이후부터**라 답변 중 4연타는 1타가 답변을 멈추고 나머지 3타가 확인창을 띄운다. CLI가 이 자리에 쓰는 `Ctrl+F`·`Ctrl+T`는 **쓰지 않았다** — IDE 안에서는 각각 Find와 Update project from VCS라 우리가 잡으면 IDE 단축키를 삼키고, `F12`로 이미 같은 일을 겪었다(#333). 패널의 `✕`와 3연타는 `useBackgroundTaskActions` 하나를 공유한다 — 같은 기능이 두 규칙을 따르지 않도록 ([#330](https://github.com/Swttch/swttch/issues/330))
- [Fix the proposed code where you are reading it](./042-editable_diff_review/en.md) — 제안된 편집의 **Proposed 쪽을 그 자리에서 고쳐 그대로 적용**하는 기능([#305](https://github.com/Swttch/swttch/issues/305)). 제보 원문은 `the proposed side is editable` 한 줄로, 사소한 수정을 에이전트에게 설명하기 아깝다는 것이었다. IntelliJ diff가 읽기 전용인 이유는 뷰어 설정이 아니라 **어느 팩토리 메서드로 콘텐츠를 만들었나**였다 — `DiffContentFactory.create`는 돌려주는 문서에 `setReadOnly(true)`를 걸고, `createEditable`만 쓰기 가능하게 남긴다(플랫폼 바이트코드로 확인: 전자는 `readOnlyDocumentContent`, 후자는 `documentContent(_, false)` 경유. 둘 다 internal·deprecated·experimental 아님). Original 쪽은 계속 읽기 전용인데, 디스크의 파일이라 타이핑해도 아무것도 안 바뀌면서 바뀐 것처럼 보이기 때문. 저장 경로는 이미 최종 텍스트 기반이었다 — `buildPartialApproval`이 `applyAcceptedRanges`로 문자열을 만든 뒤 tool input을 다시 쓰고 있어서, 그 자리에 편집된 텍스트를 넣기만 하면 됐다(**편집이 있으면 hunk 범위를 무시** — 범위는 원본 제안을 가리키므로 타이핑한 결과를 표현할 수 없다). Reject는 편집을 버린다(거절은 '다른 걸 쓰겠다'가 아니다). **standalone에는 diff가 아예 없던 것도 함께 해결** — `BrowserBridge.openDiff`가 no-op이라 브라우저에선 파일명만 보고 승인해야 했고, `GET_DIFF_PREVIEW`로 백엔드가 들고 있던 preview를 받아 [@pierre/diffs](https://diffs.com)(Apache-2.0, 이미 쓰던 Shiki 기반, 편집·라인 주석 지원)로 그린다. 515K 청크라 lazy import(초기 번들 2.6M 그대로). 답변은 IDE와 **같은 `RESOLVE_DIFF`·같은 리졸버**를 쓴다 — 경로가 둘이면 '아무것도 안 남김'의 해석이 갈리고 그 차이가 파일을 쓴다. `showDiffInIde` 설정은 '볼지 말지'에서 **'어디서 볼지'**로 의미가 바뀌었고(끄면 채팅 안 diff), 전환기 옵트인을 새 설정 없이 기존 컨트롤로 처리한 것. 브라우저 실측에서 두 건을 잡았다 — 전체 파일 재작성이 1161px로 그려져 **패널 헤더가 화면 위로 460px 밀려나** Apply/Reject에 닿을 수 없던 것(높이 제한+sticky 헤더, 폭 720px 미만은 unified), **모델이 편집을 모르는 문제도 함께 해결** — `updatedInput`은 CLI가 쓰는 내용만 바꾸고 모델의 기억은 안 바꾼다(transcript엔 모델 자신의 `tool_use`가 남고, write의 `tool_result`는 내용 없는 성공 한 줄. CLI 바이너리의 allow 분기가 `updatedInput`·`updatedPermissions`만 읽는 것까지 확인). 그래서 20000을 15000으로 고쳐도 모델은 계속 20000이라 보고했다. **공식 확장은 이 문제를 `openDiff` MCP 툴 반환값으로 푸는데 그건 모델 비노출 내부 RPC라 우리 철학상 금지**, hook의 `additionalContext`는 사용자 `settings.json`을 우리가 건드려야 하고 우리를 안 쓰는 터미널 세션에서도 계속 발동해 탈락. 남은 공식 경로인 **`<system-reminder>`로 감싼 사용자 메시지**로 즉시 전송한다(CLI 자신의 관용구이고 webview가 렌더 전에 태그를 지워 화면엔 안 보임). 다음 사용자 메시지에 묻어 보내지 않는 이유는 **대화가 편집 직후 끝나면 영영 못 알리기 때문**. 내용은 요약이 아니라 적용된 텍스트 그대로이되 **제안 대비** 차이로 좁힌다(파일 대비로 하면 편집 전체를 다시 말하게 되고, Write는 3.7KB→464B). 문구는 '응답하지 말라'가 아니라 **'굳이 답한다면 이 한 줄만'**으로 구성 — 응답이 올 수밖에 없다면 짧고 참인 쪽으로 유도하는 게 낫다. hunk만 고른 경우엔 안 보낸다(제안 내용 자체는 안 바뀌므로). 그리고 편집 중 숫자 키가 **승인 단축키로 새어나가** `15000`을 치면 `1`에서 승인되며 원본 제안이 쓰이던 것(편집 영역이 shadow root 안 contenteditable이라 `INPUT`/`TEXTAREA` 검사에 안 걸렸고, `event.target`은 호스트로 리타겟팅됨 → `composedPath()`로 판정)
- [Keeping the diff from opening on its own](./043-diff_auto_open_toggle/en.md) — 편집 승인을 물을 때 **diff를 저절로 열지 말지를 고르는** 설정([#349](https://github.com/Swttch/swttch/issues/349), 마켓플레이스 리뷰 2건). 기존 diff 설정은 전부 **어디에 열지**(`diffSurface`: IDE 뷰어/자체 페이지, `browserDiffPresentation`: 새 탭/오버레이)만 정해서 어느 값을 골라도 창은 떴다 — 그래서 '열기' 자체를 별도 축(`autoOpenDiffOnPermission`, 기본 true)으로 분리했다. **자동 열기 경로가 두 갈래라 한쪽만 막으면 다른 쪽이 대신 열어 아무것도 안 바뀐다** — 백엔드 `preparePermissionReview`(IDE 뷰어·에디터 탭)와 웹뷰 `useAutoOpenDiffReview`(오버레이·브라우저)가 `backendOpensIt`으로 서로를 배제하게 짜여 있어 **두 곳에 같은 조건을 동시에** 걸었다. 백엔드 게이트는 **`rememberPreview` 뒤·모든 opener 앞**에 둔다 — 저장까지 건너뛰면 승인 패널의 파일명 링크가 볼 게 없어져, 원치 않는 창을 없애다가 링크를 깨는 교환이 된다. 수동 클릭(`useOpenDiffReview`)은 게이트 밖이라 끄더라도 그대로 동작. 부재는 켬으로 읽어 업그레이드로 기존 동작이 사라지지 않는다. 적용 순간은 CLI가 `can_use_tool`을 보낼 때뿐이라 `acceptEdits`·`bypassPermissions`는 원래부터 무관(권한 모드를 따로 검사하는 코드는 없다)
- [Naming a chat tab yourself](./044-rename_chat_tab/en.md) — 탭 이름을 **직접 정하는** 기능([#301](https://github.com/Swttch/swttch/issues/301)). 라벨이 대화 제목을 자동으로 따라가는데 그 제목은 첫 메시지에서 나오므로, 서로 다른 작업을 하는 탭 둘이 비슷한 이름을 갖고 사용자가 개입할 수단이 없었다. 플랫폼에는 `ToolWindowTabRenameActionBase`가 있고 터미널과 제보자의 데모 플러그인이 그걸 쓰지만 **우리 환경에선 못 쓴다** — 그 팝업은 탭에 앵커된 Swing 벌룬인데 채팅 탭 안은 JCEF 브라우저라 자기 네이티브 창을 직접 그려서, 그 위에 뜬 벌룬이 **클릭도 키 입력도 못 받는다**(입력란을 누르면 뒤의 세션 드롭다운이 열리고 타이핑은 1분이 지나도 안 들어갔다. 2024.2에서 확인). 데모가 이걸 안 겪은 이유는 **탭 내용물이 순수 Swing**(`JPanel`+`JBTextArea`)이라 — 데모가 재현하지 않은 유일한 조건이 하필 이거였다. 그래서 IDE 액션은 **아무것도 리네임하지 않고** 클릭된 탭만 알리고, 그 탭의 웹뷰가 모달을 그린다(터널로 붙은 브라우저 사용자도 같은 창을 쓴다). 설계의 핵심은 **이름이 탭이 아니라 그 탭이 보고 있는 대화에 붙는다**는 것 — 탭에 붙이면 그 탭에서 여는 모든 대화가 같은 라벨을 뒤집어써서 작업을 구분하려는 목적과 정반대가 된다. 키는 **경로에서 파생**하고 별도 필드로 들고 있지 않아 실제 위치와 어긋날 수 없다. 예외가 하나 있는데 `/sessions/new`에서 이름을 짓고 **첫 메시지를 보내 시작한 대화는 그 이름을 물려받는다**(아직 id가 없어 탭에 임시로 걸어뒀다가 옮긴다 — 사용자가 이름 붙인 대상은 빈 화면이 아니라 곧 시작할 작업이므로). 값의 주인이 **웹뷰가 아니라 IDE인 이유는 lazy mount** — 채팅 패널은 클릭해야 뜨므로 웹뷰가 이름을 들고 있으면 재시작 직후 모든 탭이 이름 없이 남고, 여러 세션을 구분하려던 기능의 목적이 무너진다. `panelId`는 웹뷰가 보내는 게 아니라 **백엔드가 연결에서 꺼내** 남의 탭 이름을 바꾸는 게 구조적으로 불가능하다. 진입점은 **두 호스트 모드 모두** 만들었다 — 기본값이 EDITOR_TAB이라 툴윈도우 메뉴에만 달면 대다수 사용자가 기능을 못 본다(두 메뉴는 별개 그룹이고 클릭된 탭을 각각 `Content`와 파일로 넘겨줘 액션이 둘, 이후는 같은 요청으로 합류). 곁들여 헤더의 `HideIdLabel`을 **상수 복사본 대신 `ToolWindowContentUi.HIDE_ID_LABEL`로** 바꿨다 — 주석이 internal이라 피한다고 적어놨는데 2024.2 바이트코드를 읽어보니 `@NonNls`였고, 상수를 쓰면 플랫폼이 문자열을 바꿔도 빌드가 깨져서 알 수 있다
- [You're told when the file changes while you're reviewing](./045-review_base_changed/en.md) — 검토 중에 파일이 디스크에서 바뀌어도 **승인하면 낡은 내용이 그대로 덮어써지던** 데이터 손실 수정([#359](https://github.com/Swttch/swttch/issues/359)). 리뷰는 `can_use_tool`이 도착한 시점의 파일을 읽어 만들어지는데, 승인할 때 파일을 다시 읽지 않아서 그 사이 생긴 변경이 확인 없이 사라졌다 — 제보자는 1,090줄 파일이 1줄이 되는 것을 한 세션에 두 번 겪었고 커밋 안 한 작업을 잃었다(IDE에서 같은 파일을 병행 편집 중이었다). 특히 Write가 위험한데, Edit은 `old_string`을 못 찾으면 CLI가 스스로 실패하는 반면 Write는 파일 전체를 조건 없이 덮어써서 중간에 막아주는 것이 없었다. 해결은 두 층 — **승인 시점에 파일을 다시 읽어 다르면 CLI에 아무 답도 보내지 않고 멈추고**(CLI가 권한 요청을 무기한 대기하는 것을 실측 확인: 193초 무응답에도 살아있음), 그 전에 **IDE의 저장 이벤트로 검토 중에 미리 알린다**. 후자는 놓칠 수 있어 편의이고 전자가 보증이다. 최신화 버튼은 현재 파일 기준으로 제안을 다시 만들며, 이때 헝크 선택은 일부러 초기화한다(바뀐 base의 줄 번호를 가리키고 있어 그대로 옮기면 같은 종류의 버그가 된다). 같은 줄이 겹치면 합치지 않고 그 사실을 알린다 — git도 여기서 사람에게 넘긴다
- [Bringing the backend back without a reboot](./046-backend_reboot_recovery/en.md) — `Node.js backend failed to start / Plugin resources not ready`에 갇혀 **Windows를 재부팅해야만 풀리던** 상태를 두 축으로 해소([#308](https://github.com/Swttch/swttch/issues/308), 0.28.0에서 우연히 안 나다가 0.29.1에서 재발). 원인은 `PluginResourceExtractor`가 추출 직렬화에 쓰던 `.lock`을 **무한정 블로킹인 `channel.lock()`**으로 기다린 것 — 잔류 백엔드가 락을 쥐면 유일한 천장이 `NodeProcessManager`의 30초 리소스 게이트였다(별도 프로세스로 락을 쥔 재현 테스트로 확정). ① **락을 제거**했다. 락은 PR #151에서 클래스와 함께 들어왔고 PR 본문에도 부수 문장 한 줄뿐이며 테스트도 없었다 — **정합성은 temp+검증+rename이 담당**하고 락이 막던 건 IDE 2개 동시 기동 시 *중복 unpack 낭비*뿐이라, 그 낭비를 아끼려 사용자를 재부팅에 가둘 이유가 없다. 다만 락을 걷어내자 **가려져 있던 경쟁 경로 2개가 처음 노출**됐다: 디렉토리 rename은 대상이 비어있지 않으면 실패하고(POSIX `Directory not empty`), `clearTarget`이 **승자가 이미 서비스 중인 디렉토리를 삭제**해 #149(`Not found`)를 재생산할 수 있었다 → 양쪽 모두 **승자의 번들을 채택**하도록 처리(같은 버전이라 내용 동일). **함정**: 승자 파일의 *존재*만 단언하면 '지웠다 같은 내용으로 다시 만든' 경우를 통과시켜 무력화 검증이 뚫린다 — `fileKey`(inode)로 **정체**를 단언해야 잡힌다. ② **IDE가 죽으면 백엔드도 죽는다**로 호스트 소유권 규칙을 바꿨다(PR #254의 IDE 예외를 철회). 기존 규칙은 IDE 사망 시 바깥 접속자(브라우저·터널)가 있으면 백엔드를 살려뒀는데(모드 C), **그렇게 남은 백엔드는 IDE가 없어 끌 UI조차 없는 좀비**이고 그것이 #308의 재료였다 — 터미널 호스트엔 이미 같은 이유로 '셸이 죽으면 무조건 죽는다'가 적용돼 있었다. `server.ts`의 watchdog 분기(JetBrains=`setKeepAlive(false)` / standalone=`shutdown()`)를 `shutdown()` 하나로 합치고, 깨끗한 종료는 watchdog 10초 폴링을 안 기다리게 Kotlin `dispose()`가 `detach()` 대신 `killNow()`를 부른다. **`killNow()`는 자손을 먼저 죽인다** — 부모를 먼저 죽이면 자식이 init으로 재부모화돼 핸들을 잃고 CLI가 고아로 남는다. 회귀는 `backend-lifecycle.verify.mjs` S3을 **반대로 뒤집어** 검증(클라이언트 붙어 있어도 부모 사망 즉시 종료, 실측 0초). 잃는 것: IDE 닫고 터널로 이어 쓰는 사용법. ③ **보완**: 잔류 감지 시에만 `Retry` 옆에 `Reboot plugin backend` 버튼이 뜨고, 정리→**종료 확인**→재기동을 `BackendRebooter`/`StaleBackendReclaimer`로 캡슐화해 재사용 가능하게 뺐다. **잔류 감지와 버튼 노출은 캡슐 안에 넣지 않았다** — #308의 발동 조건일 뿐이라 넣으면 다음 호출자가 못 쓴다. 재기동은 **스폰한 주체**가 한다(IDE가 띄웠으면 IDE, 터미널이면 `ccg`)는 호스트 소유권 규칙을 계약으로 강제하고, **확인 전 재기동은 금지**. kill 대상은 **LISTEN 소켓 + 자기 PID 제외**로만 좁힌다 — bare `lsof -ti :PORT`는 그 포트에 *연결된* IDE JVM까지 반환해 IDE를 exit 137로 죽인 전례가 있다(PR #67). 포트는 짐작하지 않고 **이 IDE가 실제로 바인딩했던 `lastPort`가 있을 때만** 버튼을 띄운다
- [Collapse a reply and scroll your session by its prompts](./047-collapse_reply_per_send/en.md) — 내가 보낸 메시지 **왼쪽 여백의 접기 화살표**(에디터 코드 폴딩과 같은 자리·같은 두 상태)를 한 번 누르면 그 메시지에 대한 Claude 응답이 다음 메시지 직전까지 접힌다([#368](https://github.com/Swttch/swttch/issues/368)). 긴 세션에서 이전 프롬프트를 찾으려면 응답 벽을 전부 스크롤해야 했고 다 본 응답을 치워둘 방법이 없었다 — 몇 개를 접으면 세션이 **내가 물어본 것들의 목록**이 된다. 화살표는 대화 목록의 `px-4`만큼 `absolute -start-4`로 밖에 걸쳐 **말풍선이 밀리지 않는다**(칸을 차지하는 형태를 먼저 만들었다가 실측 후 되돌림). hover 없이 **항상 보인다** — 접힘 상태를 멀리서 읽어야 하고, 찾아 헤매는 화살표는 줄이려던 손품을 되돌려준다. 우상단 **⋮ 메뉴도 유지**한다(fork·rewind #356이 들어올 자리라, 이미 있던 항목이 사라지는 게 더 이상함). 접는 단위는 새로 계산하지 않는다: 고정 메시지 작업(#274)이 대화를 이미 "메시지 하나 + 그 응답"으로 나눠뒀고(`groupIntoSendSections`) 그 묶음의 뒷부분을 숨기는 것이 전부다. 접힌 응답은 **트리에서 빼지 않고 `display: none`으로 숨긴다** — 개수를 계속 세기 위해 의도적으로 맞바꾼 것으로, 화면에서 들어내면 셀 것이 없고 접는 순간 찍은 숫자는 스트리밍이 이어지면 낡는다(레이아웃·페인트 비용은 그대로 사라지고 React 재조정만 남음). **개수는 `[data-message-bullet]` 불렛을 쿼리셀렉터로 센다**(`ToolWrapper` + `AuthErrorRenderer` 두 곳이 유일한 발생지). 예전 "N hidden messages"는 `section.body.length`(=JSONL 엔트리 수)를 세서 화면에 4개 보이는 응답이 스트리밍 중엔 2, 새로고침 후엔 11로 나왔었다(실측: 엔트리 11개 중 7개가 `attachment`). 숨김 판정은 `getClientRects()`가 아니라 **인라인 `display:none` 조상 탐색** — jsdom은 레이아웃을 안 해서 전자를 쓰면 테스트에서 전부 0이 되고 수동 검수 외엔 검증할 데가 없어진다(탐색은 접힌 body에서 멈춘다). 어떤 묶음이 접혔는지는 위치가 아니라 **head 메시지의 uuid**로 기억해 이전 페이지 로딩이 앞에 끼어들어도 엉뚱한 응답이 접히지 않는다. 접힘 안내는 **'중단됨' 줄과 같은 스타일**(`text-[1rem] text-text-primary/60 italic`)로 좌측정렬 한 줄, 화살표 없음 — 전체폭 점선 띠(55px)는 딸린 말풍선(30px)보다 컸고 '맨 아래로 스크롤' 알약과 같은 물건으로 보였다. UI 문구는 **Collapse reply / Expand reply / Reply collapsed / N replies collapsed** 로 한 동사·한 목적어의 대칭 세트, 12개 로케일 동일 규칙 (`fix/368-fold-control` 브랜치)
- [The `${VAR}` in your MCP config now resolves to the real value](./048-mcp_env_var_expansion/en.md) — `.mcp.json`의 `env`에 적은 `${VAR}`가 **글자 그대로 서버에 전달되던** 것을 수정([#364](https://github.com/Swttch/swttch/issues/364)). `.mcp.json`은 커밋되는 파일이라 비밀값을 자리표시자로 두고 실제 값은 `.claude/settings.local.json`에 두는 것이 표준 사용법인데, 터미널에서는 되고 패널에서만 안 됐다 — 확장이 `process.env`만 봤고, CLI는 자기가 읽은 `env` 블록을 **자기 프로세스 환경에 주입한 뒤** 확장하지만 우리 백엔드는 별개 프로세스라 그 값이 없었다. 특히 조용한 실패였다: 이런 값을 받아도 서버가 죽지 않고(제보의 postgres-mcp는 경고만 남기고 기동해 handshake까지 답한다) 패널엔 초록 `Connected`로 뜨는데 화면 어디에도 이유가 없었다. **값을 18곳에서 찾는다** — `process.env` → 홈 8곳 → `~/.claude.json`의 `projects[<경로>].env` → 프로젝트 8곳 순으로 약한 것부터 병합하며, 홈과 프로젝트가 **1:1 대칭**이라 같은 모양의 파일이면 어느 층에 둬도 읽힌다. 순서는 CLI 2.1.170으로 실측하고 2.1.251로 재측정해 **두 버전이 완전히 일치**함을 확인했다. **CLI보다 넓게 읽는 것은 의도다** — 실측상 CLI는 user 스코프까지만 확장에 쓰며, 프로젝트 `settings.json`의 `env`는 자식 프로세스 환경에는 주입하면서 같은 실행의 `${VAR}` 확장에는 안 쓴다(`V9=${...}`인데 `CCG_P9=yes`로 확인). 하필 제보가 값을 둔 자리가 거기라, 그 분리와 CLI가 아예 모르는 파일들(`claude.json`·`mcp.json`·user 스코프 `settings.local.json`)을 **결함으로 보고 우리가 읽기로** 정했다. 확장 소스와 spawn 환경은 **같은 맵 하나**를 써서 둘이 갈라질 수 없다. **못 찾은 변수는 이름을 알려준다** — 서버 상세에 `Missing environment variables: X`를 경고색으로(죽은 게 아니라 잘못 설정된 것이므로), 계산은 **도구 조회가 아니라 목록 단계**에서 한다(변수 때문에 망가진 서버는 연결 자체가 안 되는 일이 많은데 도구는 연결된 서버에만 물어보므로, 도구 쪽에 실으면 정작 필요할 때 침묵한다). 곁들여 브라우저 검수 중 발견한 것 둘: **`type`을 생략한 stdio 서버**가 `switch`의 `default`로 빠져 도구 목록이 통째로 비었고(오류도 없어 "도구가 없는 서버"와 구분 불가, `.mcp.json`에서 `type`은 선택이고 제보의 설정도 그 형태 — CLI는 `case void 0: case "stdio":`로 처리한다), **`readClaudeSettings`의 얕은 병합**이 `settings.local.json`에 `env` 한 줄만 있어도 `settings.json`의 환경 변수를 통째로 날렸다(프로젝트 레벨 리더는 이미 깊은 병합이었다). 그 수정 과정에서 `readJsonFileSafe`가 "JSON으로 파싱됨"을 "설정 객체임"으로 취급하던 결함 3개가 드러났다 — `null` 파일이 `Object.keys`를 던져 **멀쩡한 다른 파일의 설정까지 날렸고**, 최상위 배열·문자열이 인덱스 키로 새어들었고, `"__proto__"` 키가 병합 결과의 **프로토타입을 갈아끼워** `Object.keys`엔 안 보이는데 `'X' in settings`가 참이 됐다(settings-migration이 정확히 그 `in`으로 이관 대상을 정한다). 배열은 이어붙이지 않고 교체하며 `null`은 삭제가 아닌 값이라는 두 정책도 **결정으로 명시**해 다음 사람이 버그로 오인해 바꾸지 않게 했다. 테스트가 `deepMergeSettings`를 **복제해서** 검증하고 있어 실제 함수에서 가드를 지워도 전부 통과하던 것도 함께 바로잡았다. 검수는 브라우저로 38건 — probe MCP 서버가 받은 값을 **도구 이름으로 노출**하게 만들어 18개 소스를 하나씩 단독 확인하고(Part A), 18개를 전부 심은 뒤 강한 것부터 지우며 값이 한 칸씩 내려오는지 확인했다(Part B, 건너뛴 단계 없음)
- [MCP servers run through Docker no longer pile up containers](./049-mcp_docker_container_leak/en.md) — `docker run`으로 도는 MCP 서버가 **패널을 새로고침할 때마다 컨테이너를 두 개씩 남기던** 것을 수정([#363](https://github.com/Swttch/swttch/issues/363), 제보자는 반나절에 67개·4.2GB). `--rm`이 붙어 있어도 소용없었던 이유는 우리가 붙잡은 것이 컨테이너가 아니라 **`docker` 클라이언트**여서다 — stdin 닫기·SIGTERM·SIGKILL 세 단계가 전부 클라이언트에게 가고, 컨테이너 안 PID 1이 EOF로도 SIGTERM으로도 안 죽으면 셋 다 살아남으며 `--rm`은 **컨테이너가** 끝나기를 기다리는 옵션이라 영영 발동하지 않는다. Docker는 누가 띄웠는지도 기록하지 않아(클라이언트를 죽이기 전후 `docker inspect`에 **바뀌는 필드가 하나도 없다**) 나중에 고아를 알아볼 방법이 없으므로, 소유권을 **띄울 때 미리 적어두는** 구조로 갔다. 세 갈래로 해결했다 — ① 상태 조회가 매번 `claude mcp list`/`mcp get`으로 CLI를 새로 부팅해 서버를 두 번 더 띄우던 것을, **이미 떠 있는 CLI에게 묻는** 방식으로 바꿔 새로고침당 신규 컨테이너를 0으로 만들었다(덤으로 **사용자의 채팅이 실제로 붙어 있는 프로세스의 상태**를 보여주게 되어 정확해졌고, 도구 목록도 같은 답변에 와서 상세 화면이 다시 접속하지 않는다. 물어볼 CLI가 없으면 공식 명령으로 그대로 폴백한다) ② Fable 가용성 점검처럼 **MCP가 필요 없는** 프로브는 서버를 아예 안 읽게 했다 ③ 정말로 서버가 필요한 경로는 spawn 직전과 **CLI 프로세스 종료 시**의 차집합 중 설정의 이미지·실행인자가 정확히 일치하는 것만 회수한다 — 기준을 세션이 아니라 **프로세스**로 잡은 이유는 한 세션이 여러 CLI보다 오래 살고(파이프 끊김·`--resume`·권한모드 변경) 그때마다 컨테이너가 하나씩 더 생기기 때문. 백엔드가 강제 종료돼 회수 코드가 못 도는 경우까지 막으려고 각 채팅 CLI가 자기 컨테이너 id를 레지스트리에 **미리 기록**하고 다음 부팅의 고아 스윕이 치우며, **살아 있는 다른 백엔드 소유 기록은 건드리지 않는다**. 설정에 docker MCP 서버가 없으면 코드가 실행조차 되지 않고, docker가 없거나 판단이 서지 않으면 항상 **지우지 않는 쪽**으로 실패한다
- [Your settings file can no longer be wiped](./050-settings_never_wiped/en.md) — 사용자의 `~/.claude/settings.json`이 **20개 키에서 3개로 줄어든** 사고를 수정([#386](https://github.com/Swttch/swttch/issues/386)). `mcpServers`·`statusLine`·`enabledPlugins`·`env` 등 **저희가 만들지도, 되살릴 수도 없는 사용자 자산**이 사라졌고, 제보자는 한참 뒤 커밋에 공동 작성자 줄이 붙는 것을 보고서야 알았다(`includeCoAuthoredBy: false`가 사라진 키 중 하나였다). 원인은 **두 결함이 겹쳐 증폭**된 것이다 — ① `writeKeyToJsonFile`이 `writeFile`로 저장해 **truncate 후 쓰기가 원자적이지 않았고**, 여러 백엔드(워크트리 dev 서버·standalone `ccg`·IDE 플러그인)가 같은 파일을 쓰므로 겹치면 짧은 payload 뒤에 긴 쪽의 꼬리가 남아 JSON이 깨진다. ② `readJsonFileSafe`가 **파싱 실패를 `{}`로 답했다** — 리더에겐 타당하지만 read-modify-write의 읽기 반쪽으로 쓰면 파괴적이라, 다음 저장이 그 빈 객체 위에 키 하나를 얹어 **읽지 못한 파일을 한 키짜리 파일로 다시 태어나게** 한다(남아 있던 3개는 재탄생 뒤 GUI가 저장한 것). 즉 **드문 사고 하나가 확정적 전체 삭제로 증폭**되며, 경합이 끝난 뒤에도 회복되지 않는다. `atomic-json.ts` 하나로 세 가지를 세우고 같은 결함이 있던 경로를 전수 전환했다: **temp+rename 원자적 쓰기**(임시 파일은 반드시 같은 디렉토리의 형제 — `rename`은 파일시스템 안에서만 원자적이라 `os.tmpdir()`은 다른 볼륨일 수 있다. 교체되는 파일의 **권한 비트를 stat→chmod로 이어받는다** — rename은 내용이 아니라 파일 자체를 갈아끼워서, 없으면 사용자가 600으로 잠근 파일이 umask 기본으로 돌아온다. Windows는 AV·인덱서가 대상을 쥐고 있을 때 EPERM/EBUSY가 나므로 짧은 백오프로 3회 재시도), **"읽을 수 없음"과 "비어 있음"의 분리**(`readJsonForUpdate`가 두 경우를 다른 값으로 답하고, 파싱 실패면 저장을 **거부하고 파일을 건드리지 않는다** — 0바이트 파일만 예외로 `ok`다. 지킬 내용이 없어 덮어써도 잃는 것이 없고, 거부하면 저장만 영영 막히기 때문. 파일은 디스크 포화·강제 종료·다른 도구로도 깨지므로 **원인과 무관하게 우리가 지우는 쪽이 되지 않는 것**이 이 가드의 목적), **경로별 쓰기 직렬화**(resolve된 경로마다 체인 — 우리 프로세스 안의 read-modify-write끼리는 절대 겹치지 않는다). 적용: `~/.claude/settings.json`·`settings.local.json`, `~/.claude.json`(MCP 비활성 목록 — 사용자의 전 프로젝트 기록이 든 최대 자산 파일), 프로젝트 `.claude/settings*.json`, 프로젝트 `.claude-code-gui/settings.json`(`catch { /* start fresh */ }`가 정확히 그 wipe를 하고 있었다), 세션 제목 오버라이드, `profile.json`, `license.json`. `settings.ts`에 따로 있던 `atomicWriteFile`도 지우고 공용 모듈로 합쳤다. **실측**: 별도 프로세스 4개 × 25회 × 5시행에서 고치기 전 **0/5**(매 시행 원본 키 전부 소실 — 이슈 그대로 재현), 고친 뒤 **5/5**(전부 보존). 테스트가 결함을 실제로 잡는지는 **가드를 무력화해 5개가 정확히 실패하는 것**으로 확인했다. **남는 것**: 서로 다른 백엔드 프로세스가 read-modify-write 창(중앙값 1.09ms) 안에서 정확히 겹치면 상대가 그 순간 새로 추가한 키가 밀린다 — 파일이 깨지지도, 기존 키가 사라지지도 않으며 다시 저장하면 반영된다. 같은 백엔드의 클라이언트끼리는 직렬화로 **동시 200쌍에서 손실 0회**이고, 순차 저장은 애초에 해당 없다(뒤에 저장하는 쪽이 앞의 결과를 읽는다). `claude` CLI 자신도 같은 방식이라 원래부터 있던 성질이고, 없애려면 파일 락이 필요해 이번에는 넣지 않았다
- [The backend no longer drops every ten seconds on WSL2](./051-wsl_backend_stays_connected/en.md) — WSL2 프로젝트에서 채팅이 **접속 후 정확히 10초마다 끊기고 다시 붙기를 무한 반복하던** 것을 수정([#384](https://github.com/Swttch/swttch/issues/384), 제보자 두 분이 각각 다른 기계에서 겪었고 한 분은 VS Code로 옮겨 갔다). 0.29.3에서 들어간 회귀다. 원인은 [#360](https://github.com/Swttch/swttch/pull/360)이 넣은 **호스트 사멸 감시가 프로세스 번호(pid)로 묻는다**는 데 있다 — WSL2에서는 IDE가 Windows 프로세스이고 백엔드는 배포판 안의 리눅스 프로세스라 **번호가 통하는 세계 자체가 다르다.** 리눅스에서 Windows pid를 물으면 항상 `ESRCH`(그런 프로세스 없음)가 돌아오는데, 감시 코드가 그것을 **죽음으로 읽어** 첫 확인(10초)에서 멀쩡히 떠 있는 PhpStorm을 죽었다고 판정하고 스스로 종료했다. 제보자가 WSL2 안에서 Windows 쪽에 직접 물어 해당 pid가 **그 시각 내내 살아 있었다**는 것까지 확인해 주셨다. 두 갈래로 고쳤다 — ① **"볼 수 없음"을 "죽음"으로 읽지 않는다**: 백엔드는 호스트가 방금 실행시켜서 존재하므로 **시작 순간에 호스트가 죽어 있을 수 없고**, 그 순간의 `ESRCH`는 죽음이 아니라 **여기서 관측 불가능한 번호**라는 뜻이다. 이제 감시를 무장할 때 한 번 확인해 보고 관측 불가면 **번호로 묻는 것을 아예 무장하지 않는다**(답할 수 없는 질문에 틀린 답을 내놓는 대신 묻지 않는다). Windows 네이티브는 호스트와 백엔드가 같은 pid 네임스페이스라 이 확인이 통과해 **전과 동일하게 무장**되며, `CCG_HOST_PID`가 애초에 도입된 Volta/nvm/fnm shim 경로도 그대로다(shim은 **어느 pid를 보는지**를 바꿀 뿐 **볼 수 있는지**를 바꾸지 않는다) ② **IDE 호스트는 번호가 아니라 연결로 지켜본다**: 번호 감시를 끄면 WSL2에서 아무도 호스트를 안 보게 되어 #308이 재발하므로, IDE가 살아 있는 내내 붙잡고 있는 `/rpc` 소켓의 소멸을 호스트 사멸로 읽는다 — 소켓은 **같은 pid 네임스페이스를 요구하지 않고**, 사이에 낀 shim이 가릴 수도 없으며, IDE가 어떻게 죽든 커널이 닫아 준다. 끊김 즉시 종료하지는 않는다(IDE 재시작은 끊었다가 새 소켓으로 돌아오므로) — **15초 안에 재접속이 없을 때만** 사멸로 판정한다. #360이 세운 규칙은 그대로 유지된다: **띄운 쪽이 사라지면 브라우저·터널 클라이언트가 붙어 있어도 백엔드는 끝나고**, 채팅용 `/ws`는 별도 소켓이라 판정에 들어가지 않으며, `/rpc`를 열지 않는 터미널(`ccg`) 호스트는 SIGHUP + pid 폴러 경로 그대로다. **검증**: **실제 Windows 11 노트북의 WSL2에서 재현하고 고쳐진 것을 확인했다** — 살아 있는 `explorer.exe`(pid 9328)를 호스트로 넘기고 그 pid가 배포판 안에 없음을 먼저 확인한 뒤, 배포된 0.29.4 번들은 **12초에 `Host process 9328 died`를 찍고 죽었고** 수정본은 두 폴링 주기를 넘겨 25초까지 살아남았다(대조군이 버그를 재현하는 것을 먼저 확인했기에 결과가 의미를 갖는다 — 첫 시행에서 번들 복사가 빠져 둘 다 5초에 죽었고, 대조군 조건이 없었으면 "수정본도 죽는다"고 오판할 뻔했다). 백엔드 1774개 전부 통과했고, 가드를 무력화하면 재현 테스트가 정확히 실패하는 것도 확인한 뒤 원복했다
- ["Stop asking me for every edit" now works](./052-stop-asking-for-every-edit/en.md) — 플랜을 **"예, 자동 승인"**으로 승인하고도 **이어지는 편집마다 승인 요청이 떠서** 수백 번을 눌러야 했던 것을 수정([#393](https://github.com/Swttch/swttch/issues/393), 제보자는 대안으로 "CLI를 쓴다"고 적었다). 서로 다른 결함 넷이 하나의 경험으로 합쳐진 것이었고, **UI가 제시한 탈출구가 전부 아무 일도 하지 않았다.** ① **고른 모드가 실행 중인 CLI에 닿지 않았다** — `--permission-mode`는 spawn 시점 플래그라 모드 변경은 다음 메시지의 재시작으로만 전달됐고, Claude가 작업하는 동안에는 무엇을 눌러도 화면 라벨만 바뀌었다. 이제 살아 있는 CLI에 `set_permission_mode`를 보내 그 턴의 남은 편집부터 적용한다(미문서화 subtype이라 **보조 최적화로만 쓰고**, 받지 않으면 기존 재시작 경로가 그대로 받는다 — `readReportedMode`가 CLI가 통보한 모드를 채택하므로 폴백에 추가 코드가 필요 없다) ② **플랜 승인의 "자동 승인"이 라벨만 바꿨다** — `setInputMode`가 로컬 상태만 건드렸고 CLI에는 모드 언급이 없는 평범한 승인만 갔다. 이제 같은 경로를 타며, CLI 자신의 플랜 승인 모드 변경이 우리 전환을 덮지 않는 것을 실측으로 확인했다 ③ **"예, 이번 세션에서 모든 편집 허용"을 CLI에 알리지 않았다** — 웹뷰가 도구 이름을 ref에 담아두고 이후 요청에 대신 답하는 방식이라 **CLI는 계속 물었고 질문이 답해진 게 아니라 가려졌으며**, 렌더가 바뀌면 사라졌고, 마침 물어본 도구 하나에만 걸렸다(Claude는 `Edit`·`Write`·`MultiEdit`·`NotebookEdit`를 번갈아 쓴다). 이제 문서화된 `updatedPermissions`로 승인과 함께 보내 **CLI가 스스로 묻지 않게** 되고, ref와 세션 전환 시 초기화와 조용한 자동 승인 분기를 **전부 삭제**했다. 편집 도구는 편집 계열 전체를 허용하고 그 외 도구는 자기만 허용해 **"모든 명령어 허용"이 이웃으로 넓어지지 않는다** ④ **두 diff 리뷰 어디에도 탈출구가 없었다** — 수백 번 Apply를 누르던 제보자가 IDE 리뷰 바에서 찾은 것은 `Clear all`이었고 그것을 탈출구로 읽었는데 **실제로는 체크를 전부 해제하는 정반대 버튼**이다. 두 리뷰 모두 `Allow all edits` 버튼을 갖게 했고, 열린 요청을 `Apply`와 똑같이 답하면서 세션 규칙을 함께 건다(IDE에서는 이 diff를 답하는 버튼들과 떨어뜨려 **왼쪽 정렬**). **검증**: 브라우저와 샌드박스 IDE에서 두 리뷰 화면 모두 실측했고, 버튼을 누른 뒤 **두 번째 파일이 승인 없이 쓰이는 것**과 **Bash는 여전히 묻는 것**을 확인했다. 백엔드 1779개·웹뷰 2872개 통과했고, 모드 전달을 무력화하면 신규 테스트가 정확히 실패하는 것도 확인한 뒤 원복했다. **남은 한계**: 승인 요청이 떠 있는 동안에는 입력창이 가려져 모드 선택기에 손이 닿지 않는다
- [Up and Down walk the prompts you typed](./053-prompt_history_in_the_composer/en.md) — 입력창에서 위 화살표를 눌러 이전 프롬프트를 꺼내는 기능을 실제로 쓸 수 있게 고쳤다. **다시 연 대화에서는 대개 아무것도 나오지 않았다** — 히스토리를 채팅 화면이 들고 있는 대화 기록에서 만들었는데 그것은 한 페이지, 최신 **엔트리** 50개뿐이고 엔트리는 대개 프롬프트가 아니기 때문이다(이 저장소 대화 119개 실측: 사람이 친 프롬프트 3,190개 중 최신 페이지 안에 든 것은 129개 **4%**, 가장 큰 대화는 프롬프트 193개 중 **0개**). 이제 대화 전체를 들고 있는 백엔드에 물어보며, 한 번에 20개씩 받고 거슬러 올라가면 이어서 받는다(프롬프트에는 붙여넣은 것이 그대로 실려 있어 100개를 한 번에 보내면 최악의 대화가 **21MB** — 이미지 블록을 빼는 것은 원본 데이터 보존 원칙이 금지하는 엔트리 편집이므로 **개수가 아니라 범위를 자른다**). 함께 고친 것 셋 — ① **CLI가 쓴 글이 입력창에 들어오던 것**(9,326줄 대화 하나에서 `user` 엔트리 1,345개 중 1,281개가 도구 결과이고 사람이 친 것은 46개 남짓): CLI가 표시해두는 엔트리는 플래그로, `<command-name>` 전개·명령 출력·중단 표시·`<task-notification>`처럼 **아무 플래그도 없는 것**은 내용으로 걸러낸다(`permissionMode`는 CLI가 **처리한** 프롬프트에 붙는 것이라 CLI 자신이 주입한 것에도 붙는다 — 그래서 받아들이는 근거가 아니라 걸러내는 근거로만 쓴다) ② **응답 중에 쳐서 큐에 들어간 메시지가 통째로 빠지던 것**: CLI가 그런 메시지는 `user` 엔트리로 아예 쓰지 않고 큐 기록만 남기기 때문으로, 채팅 기록이 말풍선을 되살릴 때 쓰던 짝짓기 규칙을 `shared/`로 올려 **두 곳이 같은 함수를 쓰게** 했다(화면에 보이는데 히스토리엔 없는 메시지가 생길 수 없다) ③ **긴 프롬프트에서 커서가 어디 있든 곧장 이전 프롬프트로 건너뛰던 것**: 줄바꿈 문자를 찾는 방식이라 폭에 맞춰 접힌 한 문장은 모든 줄이 "첫 줄"로 판정됐다 — `utils/domSelection`이 Cmd+화살표에서 같은 이유로 이미 걷어낸 방식이었다. 이제 **커서가 맨 첫 글자(아래는 맨 끝 글자)에 닿았을 때만** 히스토리로 넘어가고, 불러온 프롬프트는 걸어가던 방향의 끝에 커서를 놓아 연타가 이어진다. 방금 보낸 프롬프트는 CLI가 기록하기 전에도 즉시 잡히고, 입력창을 비우면 걷던 위치가 처음으로 돌아간다. **Shift+화살표는 선택 제스처이므로 히스토리가 가져가지 않는다** — 판정이 캐럿 위치만 보고 수식 키를 안 봐서, 불러온 프롬프트가 커서를 놓고 가는 맨 첫 글자에서 Shift+Up을 누르면 선택 대신 프롬프트가 통째로 바뀌었다([#396](https://github.com/Swttch/swttch/issues/396), 실측: 210자 → 206자). **남긴 한계**: 같은 프로젝트의 다른 대화 프롬프트는 내놓지 않으며(CLI 터미널 히스토리는 넘나들지만 지금 안 보는 대화의 문장이 튀어나오는 것은 도움보다 놀라움에 가깝다는 판단), 슬래시 명령은 대화 기록에 XML 전개형으로 저장돼 있어 뺐다 (자체 기획)
- [Every built-in tool now has a card, including the ones under their old names](./054-tool_cards_for_every_builtin_tool/en.md) — 내장 도구 호출이 이름 옆에 **`unknown`** 한 단어만 남기고 아무것도 안 알려주던 것을 수정([#401](https://github.com/Swttch/swttch/issues/401)). 채팅은 도구를 **이름으로** 카드에 연결하고 모르는 이름은 맨 헤더로 떨어지는데, `ScheduleWakeup`·`ReportFindings`·cron 3종·워크트리 2종·MCP 리소스 3종·`RemoteTrigger`·`SendUserMessage` **열두 개에 카드가 없었다**. `/loop`를 돌리면 화면이 똑같은 `unknown` 줄만 세로로 쌓여, 얼마나 기다리기로 했는지도 왜 그랬는지도 볼 수 없었다. 고치다가 **눈에 안 띄는 두 번째 구멍**이 나왔다 — CLI 바이너리 안에 **옛 도구 이름을 지금 이름으로 바꾸는 표**가 있고(`KillShell`·`KillBash`→`TaskStop`, `BashOutput`·`BashOutputTool`·`AgentOutput`·`AgentOutputTool`→`TaskOutput`, `ListPeers`→`ListAgents`, `Brief`→`SendUserMessage`, `ListMcpResources`·`ReadMcpResource`→…`Tool`), 옛 CLI로 기록된 대화는 **왼쪽 이름을 그대로 재생**하는데 그중 등록돼 있던 짝은 `Task`/`Agent` 하나뿐이라 **나머지 열 개도 `unknown`이었다**(지금 CLI만 써서는 영영 안 보이는 결함이라, 표는 바이너리 문자열에서 직접 읽어냈다 — macOS `grep`은 ugrep이라 긴 문맥 정규식이 "exceeds complexity limits"로 죽어 `tr -c '[:print:]' '\n' | rg`로 뽑는다). 이름이 둘 이상인 카드는 이제 **`toolUse.name`으로 제목을 달아** 대화에 적힌 단어 그대로 읽힌다(하드코딩하면 사용자가 로그에서 본 이름과 화면이 어긋난다). **세 번째 결함은 카드를 만들고 브라우저 검수를 하다가 드러났다** — 도구 인자는 글자 단위로 도착하므로 스트리밍 중에는 **아직 안 온 인자와 안 보낸 인자가 구분되지 않는데**, 없는 인자를 의미로 읽던 카드들이 그 의미를 사실처럼 말했다(리뷰 카드가 2건을 보고하는 중인 호출에 **"No findings"**). `isInputSettled()`가 그런 단정을 전부 게이트하며, 같은 결함이 있던 **기존 `NotebookEdit`**(내용이 오는 중인 셀을 "[No content]"로 부름)도 함께 고쳤다. **검수는 실제 호출로** — 격리한 일회용 git 저장소에서 cron 2종·MCP 3종·워크트리 2종·`RemoteTrigger`·`ReportFindings`·`ScheduleWakeup` 2종·`CronList`를 하나씩 불러 화면을 확인했고, `SendUserMessage`와 옛 이름 열 개는 **현재 CLI가 만들어내지 못해 테스트로만 덮인다는 것을 한계로 명시**했다. **"다 고쳤나"를 두 번 더 묻자 축을 넓혀 두 개를 더 찾았다** — ① **RTL**: 새 카드의 코드성 값 중 **cron 작업 id만 `dir="ltr"`이 빠져** 화면 미러링에서 순서가 뒤집혔다(RTL을 실제로 켜고 확인, cron 표현식·URI·`파일:줄`·워크트리 경로는 정상). 코드값 전부에 대한 RTL 테스트를 GlobRenderer가 세운 모양으로 추가 ② **동적 번역 키**: 지적의 `outcome`을 **모델이 준 값 그대로 번역 키에 끼워 넣어**, 스키마 밖 값이 오면 `reportFindings.outcome.…` 라는 **키 문자열이 화면에 뜰** 수 있었다 — 주변 렌더러들은 이미 `{defaultValue: …}`로 막고 있었고(`SearchFileRenderer`·`SearchTextRenderer`·`ResultRows`) 그 관습을 안 따른 것이라, `ReportFindings`와 **같은 누락이 있던 기존 `JetBrainsActionRenderer`**(patch op)를 함께 고쳤다. 반대로 **결함이 아니라고 확인하고 넘긴 것도 기록한다** — 권한 훅의 `assessRiskLevel`이 새 툴을 전부 `low`로 분류하지만 **`riskLevel`을 소비하는 코드가 하나도 없어** 화면에 영향이 없고(확인 전에 "위험도 누락"으로 이름 붙였으면 오진), `SkillRenderer`의 `?? t('skill.fallback')`은 폴백이 "Skill"이라 **틀린 정보가 아니며**, 기존 boolean 배지들은 전부 `=== true` 방향이라 부재를 의미로 읽지 않는다. Kotlin과 백엔드는 툴 이름을 아예 모른다. **검수는 12개 로케일 중 한국어·독일어를 화면에서 확인**(`반복 세션 한정`·`루프 중단`·`1건 발견`·`수정됨` / `1 Befund` 단수형·`behoben`)하고, 헤더를 `toolUse.name`으로 바꾼 **기존 3개 카드(`TaskOutput`·`TaskStop`·`ListAgents`)가 회귀하지 않았는지 실호출로 확인**했다. **"고칠 게 없을 때까지 고쳐라"에 이르러 마지막 결함이 나왔다** — 옛 이름을 **화면에서 볼 방법이 없다고 한계로 적어뒀는데**, 웹뷰는 세션 JSONL을 읽어 그리므로 **옛 CLI가 기록했던 모양의 대화록을 만들어 재생하면 된다**(합성 대화록이라는 점은 한계로 명시). 그렇게 띄우자 `BashOutput`·`BashOutputTool`·`KillShell`·`KillBash` 네 개가 **id가 빈 채로** 떴다 — 옛 이름은 작업 id를 `task_id`가 아니라 **`shell_id`/`bash_id`에 담는데**(`shell_id`는 지금 TaskStop 스키마에도 deprecated로 살아있다) 이름만 등록하고 **렌더러가 읽는 필드는 그대로 뒀기** 때문. CLI 자신이 하는 `task_id ?? shell_id` 방식으로 맞췄다. **어떤 테스트도 옛 필드를 보낼 생각을 못 해서** 112개가 전부 통과하고 있었다. **개발자님이 같은 세션을 CLI로 열어 나란히 놓고 네 가지를 더 지시했다** — ① 리뷰 카드 제목을 `ReportFindings`가 아니라 **`Code review`**로(CLI가 그렇게 쓴다. `notebookEdit.title`·`todoWrite.title`이 이미 같은 방식이라 12개 로케일 번역), 헤더 순서도 `high · 1 finding`으로 ② **거부된 도구가 실행된 것처럼 보이던 것** — 원인은 `DeclinedNote` 컴포넌트가 **이미 있는데 MCP 카드만 쓰고 빌트인 카드는 아무도 안 쓴** 것이라, 거부 문구가 성공 출력과 같은 결과 상자에 떨어졌다. `toolResultText`가 거부면 빈 문자열을 주게 하고(**37개 사용처 중 판정에 원문이 필요한 2곳만 `toolResultRawText`로 교체**) `ToolWrapper`가 한 곳에서 오류색으로 그린다(MCP 카드는 자체 배지가 있어 `ToolRenderer`가 `isMcpToolName`으로 제외). 불릿도 오류색으로 바꿨는데, 기존 주석이 "거부는 실패가 아니니 **의도적으로** 에러 스타일을 안 쓴다"였으므로 **의도적 번복**임을 남긴다 ③ **거부 문구를 우리 번역으로 갈아치우던 것** — `"You declined this tool."`은 내가 지은 게 아니라 기존 `tool.declined` 키였지만 지적이 옳았다. 이제 sentinel(`U+200B`)만 벗기고 **CLI 원문 그대로** 보여준다. 근거는 개발자님 판단 그대로 — *"cli 출력을 전달해준 것일뿐, 그 에러메세지가 무슨 언어인지까지 우리가 책임지지는 않으니까"* (미사용이 된 번역 키 2개는 12개 로케일에서 제거, `shared/tool-decision.ts`는 webview·backend 1:1 동기화) ④ `TaskStop`이 `{message,task_id,task_type,command}`를 **raw JSON으로 덤프**하던 것을 `TaskOutput`과 같은 모양(메타 줄 + 메시지)으로. **파싱 실패 시 원문 폴백**을 반드시 둔다(평문 오류·잘린 JSON). 웹뷰 3,167개 통과, 등록·스트리밍 게이트·RTL·번역 폴백·레거시 필드·거부 표시 여섯 가지를 각각 무력화해 정확히 그 테스트만 실패하는 것도 확인
- [Past conversations now open a page at a time](./055-session_list_loads_a_page_at_a_time/en.md) — 세션이 쌓인 프로젝트에서 지난 대화 목록이 뜨는 데 4.9초, 다른 작업이 함께 돌면 20초를 넘기던 것을 첫 화면 0.3초로 줄였다(자체 기획). **체감의 정체는 스캔 속도가 아니라 같은 일을 여러 번 한 것**이었다 — 화면 한 번에 `GET_SESSIONS`가 **3회** 갔고(“하위 폴더 대화 포함” 설정이 **어느 폴더를 훑을지**를 정하는데 화면이 그려진 뒤에 도착해, 좁은 범위로 2.3초를 훑어 버리고 넓은 범위로 다시 훑었다), `JSON.parse`가 CPU를 잡는 작업이라 load average 21.59인 실사용 조건에서 몇 배로 늘어났다. 고친 것 넷 — ① **제목이 정해지면 읽기를 멈춘다**: 목록에 필요한 다섯 값 중 넷은 파일 앞부분이 정하는데 끝까지 읽고 있었다(첫 프롬프트가 시스템 프롬프트를 함께 담아 **한 줄이 median 3.2MB**, 파일 중앙값이 3.27MB라 **절반은 제목 하나에 파일 전체를 읽는 셈**). `lastTimestamp`는 파일 끝 64KB에서 얻고(497/497 전량 스캔과 일치, 259개 47ms), 창이 부족하면 1MB·8MB로 넓힌다 — **폴백이 없으면 마지막 엔트리가 창보다 큰 세션이 파일 맨 앞 시각으로 떨어져 목록 아래로 밀린다**(무력화해 실패를 확인). `summary`는 조기 중단으로 잃지 않도록 tail에서도 찾는다(이 머신 **506개 전부 0건**이고 컴팩트는 `isCompactSummary`/`compactMetadata` 필드로 기록되지만, 구버전 파일을 위해 남긴다) ② **범위가 정해지기 전엔 목록을 부르지 않는다**(3회 → 1회). 설정 프로바이더가 없거나 조회가 실패한 경우는 “대기”로 보지 않아 **목록이 영영 안 뜨는 교착이 없다** ③ **화면에 보이는 만큼만 연다**: 정렬은 tail로 전체를 확정한 뒤 제목은 30개만 읽고, 스크롤하면 이어 받는다(254개 → 30개). **mtime 정렬은 쓸 수 없다** — CLI가 대화가 아닌 엔트리(`file-history-snapshot`·`queue-operation`)를 덧붙여 파일은 움직이는데 대화는 그대로라, 489개 중 **335개(68%)** 순위가 어긋나고 하나는 **205칸** 밀린다. 이어받는 위치는 반환된 행 수가 아니라 **백엔드가 멈춘 지점**을 쓴다(걸러진 세션이 행을 채우지 않고 커서만 전진시키므로 행을 세면 겹쳐 읽는다) ④ **모르는 것을 안다고 말하지 않는다**: 로딩 중에 “지난 대화가 없습니다” 대신 “불러오는 중”. **단위 테스트 3,187개가 통과하는 상태에서 브라우저 검수가 둘을 더 잡았다** — 닫힌 드롭다운이 페이지를 계속 요청했고(레이아웃 전 박스는 스크롤 수치가 전부 0이라 “행이 모자라다”로 읽힌다, 스크롤 없이 offset이 **31 → 125 → 156**), 검색이 스크롤 로더와 경쟁해 30개씩 네 번 요청하는 동안 한 번에 가져오라는 요청이 차례를 못 받았다(둘 다 **백엔드 로그에 요청 파라미터를 찍어야** 보였다). **시도했다가 되돌린 것도 남긴다** — `getProjectsList`(프로젝트 선택 화면·작업 디렉토리 트리·중첩 목록 셋이 공유, 278~371ms)의 `cwd` 추출을 파싱 대신 문자열 스캔으로 바꾸려 했으나, **정규식 9ms는 구조를 안 읽어서 빠른 것이고 그래서 중첩 `cwd`를 최상위인 양 반환**하며, 정확한 깊이 추적 스캐너는 **126~137ms로 `JSON.parse`(92~105ms)보다 느렸다**(cwd 라인 3.26MB가 **전부 문자열 리터럴 안**, 그 안 이스케이프 따옴표가 수만 개). 원복하고 **계약을 고정하는 테스트 5개만 남겼다**. `messageCount`는 세려면 파일을 끝까지 읽어야 해 더 이상 계산하지 않고 `null`이며(0으로 채우면 “엔트리 없는 세션”과 “아무도 안 센 세션”이 같은 값이 된다), 화면에 표시되던 곳이 없어 보이는 변화는 없다
- [Go back to any message: fork the conversation, rewind the code](./056-fork_and_rewind/en.md) — 대화를 앞으로만 갈 수 있던 것을 고쳐, 보낸 메시지마다 ⋮ 메뉴에서 **되돌아갈 수 있게** 했다([#356](https://github.com/Swttch/swttch/issues/356)). 터미널에서는 Esc 두 번으로 되던 것이 IDE에는 없었다. 세 항목이다 — **여기서 대화 포크**(그 메시지 이전을 공유하는 새 세션을 열고, 포크한 메시지는 입력창에 되채워 고쳐 보낼 수 있게 한다. 원본은 그대로 남는다), **여기로 코드 되감기**(Claude가 편집 전에 떠둔 사본에서 파일만 복원하고 대화는 건드리지 않는다), **둘 다**(파일을 먼저 복원하고 성공했을 때만 새 세션을 연다 — 되돌려지지 않은 코드를 새 가지에서 마주하지 않도록). **되감기는 공식 CLI가 한다.** `--rewind-files <메시지>`이며, `--help`에 없는 플래그라 **원칙 예외로 기록하고 채택**했다([문서](../principle-exceptions/356-rewind-and-fork-hidden-cli-flags.md), `/rewind`는 `supportsNonInteractive:false`라 헤드리스에서 못 쓴다). **포크는 플러그인이 원본 트랜스크립트를 포크 지점까지 새 파일로 복사한다.** 처음엔 이것도 CLI의 `--resume-session-at <엔트리> --fork-session --session-id <새 세션>`으로 만들었지만 되돌렸다. CLI는 세션을 만들 때 반드시 첫 메시지를 함께 받아, 빈 세션을 만들 방법이 없다는 것을 세 가지로 실측했다(빈 프롬프트는 거부, stdin을 닫으면 SessionStart 훅만 돌고 아무것도 안 씀, 대화형 재개는 "Provide a prompt to continue"). 그래서 사용자가 쓴 적 없는 메시지를 시스템리마인더로 주입해 봤는데, API 왕복과 포크당 약 10초가 들었고 그래도 매번 새 가지 맨 위에 답장이 남았다. 줄을 그대로 옮기기만 하고 `uuid`·`type` 이상은 읽지 않으므로 메시지들은 원래 신원을 유지하고 원본 세션은 한 바이트도 변하지 않는다. 이 판단의 배경은 [예외 문서](../principle-exceptions/356-rewind-and-fork-hidden-cli-flags.md)의 "포크는 트랜스크립트를 직접 쓴다" 절에 남겼다. **되감기가 성립하려면 파일 사본이 있어야 하는데, 우리 spawn 방식이 그것을 꺼뜨리고 있었다** — CLI는 대화형 REPL에서 `fileCheckpointingEnabled`(기본 true)를 읽지만 헤드리스에서는 다른 분기를 타 환경변수 없이는 백업을 아예 안 쌓는다(대조 실측: 백업 0개·스냅샷 엔트리 0개, `--rewind-files`는 "File rewinding is not enabled."로 종료). 즉 **터미널 사용자가 그냥 되는 것을 우리가 못 하게 만들고 있었던 것**이라, 공식 설정 키를 그대로 **설정 → 일반 → 파일 체크포인트**에 노출하고 그 값으로 주입 여부를 정한다(새 키를 발명하지 않아 터미널에서 끄면 여기서도 꺼진다. 기본값이 `true`라 폴백도 `?? true`다 — 주변 토글들과 반대라 주석에 이유를 남겼다). **곁가지로 원본 데이터 보존 원칙 위반을 하나 고쳤다** — `filterActiveChain`이 통과시킬 타입을 이름으로 나열하고 있어 **uuid 없는 엔트리 7종 중 3종만 살아남고 나머지는 웹뷰에 아예 도달하지 못했다**(세션 120개 실측: `last-prompt` 1,967 · `atis-latch` 1,041 · `mode` 608 · `pr-link` 321 · `ai-title` 49 · `file-history-snapshot` 6 · `permission-mode` 2). 되감기 판정에 필요한 스냅샷 엔트리가 여기 걸려 있었고, 이름 목록 대신 **"uuid가 없으면 이 필터가 판단할 대상이 아니다"**로 규칙 자체를 바로잡았다. **버전 함정 두 개를 실측으로 확정했다** — ① 백엔드가 spawn하는 claude(2.1.261)와 셸의 claude(2.1.170)가 달라, **틀린 버전으로 되감으면 `exit 0`에 "Files rewound to state at message ..."를 출력하면서 파일을 하나도 안 바꾼다**(종료 코드만 보는 검증은 깨진 되감기를 정상으로 판정한다. 실제로 이것 때문에 "동작하지 않는다"고 한 라운드 오판했다) ② 2.1.170은 Write 직후 `isSnapshotUpdate:true` 엔트리를 쓰지만 2.1.261은 안 쓰면서도 되감기는 정상이라, **스냅샷 내용으로 판정하면 안 되고** CLI 자신처럼 **엔트리 존재**만 봐야 한다. **브라우저 검수 중 세 번째 결함이 나왔다** — 메뉴가 보낸 직후에는 안 뜨고 새로고침해야 떴는데, 원인은 **웹뷰가 스트리밍 중 자체 id(`msg-<시각>-<난수>`)를 붙이기 때문**이다(CLI는 사용자 메시지를 stdout으로 되돌려주지 않아 진짜 uuid는 디스크에서만 온다). 두 동작 모두 그 id를 명령행에 넘겨야 하고 CLI는 로컬 id에 "not a user message in this session"으로 답하므로, **실패할 버튼을 보여주는 대신 기록되기 전까지 감춘다**(`isRecordedSend`). **포크를 만든 뒤 여는 경로에도 결함이 있었다** — 새로 만든 세션을 등록하는 `addNewSession`이 이미 `navigateToSession`으로 화면을 옮기는데, 포크 쪽이 그 뒤에 라우터 `navigate()`를 한 번 더 불렀다. 히스토리가 한 칸 더 쌓이고(브라우저에서 실측: 원래 1이어야 할 증가량이 2), `rootDir` 쿼리가 빠져 포크 직후 세션 목록 스코프가 조용히 좁아졌다. `navigateToSession`이 라우터 state를 받는 `handoff` 인자를 갖게 해 포크한 메시지를 입력창에 싣는 것까지 그 한 경로로 합쳤다. 목록에 뜨는 이름도 고쳤다 — 포크한 메시지가 작업 알림처럼 텍스트로 못 읽히는 것이면 "No title"로 떴는데, 브랜치 파일은 어차피 원본 앞부분을 그대로 복사한 것이라 목록이 새로고침되면 원본과 같은 제목으로 바뀐다. 그 사이 시간 동안만이라도 최종 제목과 같게 원본 세션의 제목을 그대로 물려받게 했다. **검수**: 실제 대화에서 히스토리 증가량이 1인 것, 포크 직후 목록에 원본과 같은 제목으로 뜨는 것, 원본 세션이 변하지 않는 것을 화면과 파일로 확인했다. `forkSessionHandler`에 새 테스트를 붙였고, 경로 가드는 실재하는 파일을 가리켜 가드를 무력화하면 정확히 그 테스트만 실패하는 것을 확인한 뒤에야 유효한 검증으로 인정했다(존재하지 않는 경로를 가리키면 가드 유무와 무관하게 "파일 없음"으로 통과해버려 검증이 되지 않는다). 백엔드 1,965개·웹뷰 3,213개 통과
- [Account pools: a usage limit no longer ends the conversation](./057-account_pool/en.md) — 풀 구성과 전환 순서, 전환 전 저장 계정 사용량 조회, 모두 소진 시 가장 빠른 리셋 계정의 기존 자동재개 예약, 지연·조회 실패 시 현재 계정 유지.
- [The microphone tells you why it cannot record](./058-dictation_needs_a_claude_login/en.md) — 녹음 버튼을 누르면 안내 대신 **자바스크립트 예외 원문**(`Cannot read properties of undefined (reading 'accessToken')`)이 인풋배너에 그대로 찍히던 것을 수정([#355](https://github.com/Swttch/swttch/issues/355)). 제보자는 그게 무슨 뜻인지 채팅에서 Claude에게 물어본 뒤 이슈를 올렸고, 요구는 "어떻게든 확인을 하거나, 더 나은 오류 메시지를 달라" 한 줄이었다. 원인은 **받아쓰기가 로그인이 남기는 OAuth 토큰으로만 열린다**는 것이다. `ANTHROPIC_API_KEY`로만 인증한 기기에는 그 토큰이 없어 `credentials.claudeAiOauth.accessToken` 읽기에서 TypeError가 나는데, 킷의 가용성 판정 함수는 **자기 에러 타입에만 false를 돌려주고 이 TypeError는 되던져서** 우리 쪽 `errorKind: 'unknown'` 경로로 새어 원문이 화면까지 갔다. 문제는 두 겹이었다. ① **아무것도 미리 확인하지 않았다**: `GET_DICTATION_AVAILABILITY` 핸들러가 백엔드에 이미 쓰여 있었는데 **웹뷰가 한 번도 호출하지 않는 죽은 코드**였다(전역 grep 결과 호출부 0곳) ② **실패를 날것으로 통과시켰다**: 킷 미설치가 아닌 것은 전부 `unknown`으로 묶여 예외 문자열이 그대로 배너 문구가 됐다. 조용한 세 번째 결함도 있었다. 그 availability 핸들러가 **모든 예외를 잡아 `kit_missing`이라고 답해서**, 킷이 멀쩡히 설치·최신인 API 키 사용자에게 **이미 가진 것을 설치하라고 안내할** 상태였다. 해결은 네 갈래다. 스트림을 열기 **전에** 인증을 확인해 `not_logged_in` 코드로 거절하고(마이크도 소켓도 열지 않으므로 한 글자도 못 받아쓸 세션에 녹음 표시등이 켜지지 않는다), 웹뷰가 그 코드로 **12개 로케일 번역 문구 + `로그인` 버튼**을 띄우고(같은 상단 배너가 쓰는 로그인 경로 재사용), 누르기 전에도 **마이크를 흐리게** 만들고, 설정 → 일반 → 음성 입력이 **킷은 멀쩡한데 로그인이 없는 상태**를 문장으로 적으며 하위 행을 흐린다(켜기/끄기 토글은 살려둔다. 음성 입력을 꺼둔 사람에겐 그게 돌아오는 길이기 때문이다). **마이크를 숨기지도 비활성화하지도 않은 것은 의도다.** 숨기면 왜 못 쓰는지 들어야 할 바로 그 사람에게서 기능이 사라지고, `disabled`면 누름이 삼켜져 이유가 아무도 올려볼 필요 없는 툴팁에만 남는다. **"그럼 `x-api-key`로 구현해야 하지 않나"는 조사 끝에 기각했다.** 공식 CLI 바이너리에 이 상황 전용 실패 문자열 `[voice_stream] No OAuth token available`이 박혀 있고, 소켓 헤더는 `Authorization: Bearer` 하나뿐이며, **다른 엔드포인트에는 API 키를 얼마든지 보낼 수 있는데도 받아쓰기에서는 연결 전에 물러선다.** 여기서 우리만 붙여 보내는 것은 CLI 동등성이 아니라 발명이다. **다만 서비스가 `x-api-key`를 받아줄지는 측정된 적이 없다**(어떤 클라이언트도 물어보지 않는다). 그래서 "API 키로는 못 한다"는 **모든 Claude Code 클라이언트에 대한 사실**이지 서비스에 대한 주장이 아니며, 이 구분을 `DictationErrorKind.NOT_LOGGED_IN` 주석에 **UNVERIFIED로 명시**해 다음 사람이 API 키 경로를 넣기 전에 엔드포인트를 먼저 재도록 했다(처음엔 이 구분 없이 "서비스가 OAuth만 받는다"고 단정한 주석을 넣었다가, 근거가 없다는 지적을 받고 되돌렸다). 새 테스트 22개는 전부 **수정을 하나씩 되돌려 정확히 그 테스트가 붉게 뜨는 것을 확인한 뒤**에야 통과를 인정했고, 그 과정에서 `MicButton` 단언이 기본 클래스의 `disabled:opacity-50` 때문에 **아무것도 안 흐려도 통과하던 것**을 `classList.contains`로 바로잡았다. 검수는 브라우저로 강제 상태와 정상 상태를 각각 확인(흐린 마이크·배너·설정 문구가 뜨는 것과, 강제를 풀면 셋 다 사라지고 정상 녹음으로 들어가는 것). 백엔드 1,972개·웹뷰 3,228개 통과
- [Every asset you attached, in one place](./059-assets/en.md) — 세션에 첨부한 이미지를 **방향키로 넘겨보고**, 상단 도크의 **에셋** 화면에서 메시지 단위 타임라인으로 모아 본다. 기존에는 이미지를 크게 띄워도 거기서 끝이라, 옆에 같이 붙인 것을 보려면 닫고 옆 썸네일을 다시 눌러야 했고, 과거의 이미지를 찾으려면 대화를 거슬러 올라가는 수밖에 없었다(트랜스크립트가 페이지 단위로 로드되므로 웹뷰는 세션 전체 이미지를 알지도 못했다). 백엔드에 **좌표 색인**을 새로 뒀다 — JSONL의 이미지에는 고유 ID가 없고 `message.content` 배열 안 인라인 base64라, 안정적인 것 둘(엔트리 `uuid` + 배열 위치)을 묶어 좌표로 만든다. 목록 응답에는 **base64를 싣지 않는다**(실측: 695KB어치 9장의 색인이 1KB). 바이트는 뷰어가 그 자리에 도착할 때, 썸네일은 스크롤로 눈에 들어올 때 한 장씩 가져온다. **후원 경계는 '보는 것'이 아니라 '넘기는 범위'다** — 에셋 화면과 모든 썸네일은 후원 전에도 열리고, 잠기는 것은 한 장을 띄운 채 세션 전체를 계속 넘기는 동작이며, 메시지의 끝에 닿으면 남은 장수를 숫자로 알려 후원으로 초대한다(`showSponsorGatedToast`의 톤을 따라 페이월이 아닌 제안). 게이트 판정은 훅 한 곳에 모아 뷰어 자체는 게이트의 존재를 모른다 — 그래서 채팅에서 열든 에셋 화면에서 열든 규칙이 같다. **백엔드에는 후원 게이트를 두지 않았다**: 무료 사용자도 썸네일로 모든 이미지를 보는 것이 정해진 경계라, 여기를 막으면 그 화면이 빈칸이 된다(`ensureSponsor`가 말하는 '패턴 B'). 사용자 첨부만 모으고 `tool_result` 안 이미지는 제외하며, 포크·리와인드로 버려진 갈래의 이미지는 자동으로 빠진다. 작업 중 같은 종류의 실수를 두 번 했다 — **같은 것을 여는 경로가 둘인데 하나만 고친 것**(입력창 라이트박스가 복제본이라 채팅만 고쳐졌고, 도크 아이템의 ⋮ 메뉴 행이 `useDockItemActions`로만 동작하는데 아이콘에만 핸들러를 넣었다). 후자는 **모든 `DockItemId`에 액션이 있는지 enum 전체로 단언하는 테스트**를 넣어 재발을 구조적으로 막았다. 곁다리로 기존 버그 하나 수정(닫기 버튼이 backdrop 위에 있는데 stopPropagation이 없어 `onClose`가 두 번 불렸다). **뷰어 하단은 패널로 만들었다** — 격자(에셋 화면)·확대·축소·새 탭·카운터·복사·다운로드. 우클릭 컨텍스트 메뉴를 새로 만드는 대신 **브라우저/IDE 기본 메뉴를 그대로 두고** 같은 액션에 보이는 자리를 준 것이다. 저장과 새 탭은 **blob URL 경유** — 브라우저가 다운로드용 `data:` href 길이를 제한하고 top-level 이동은 아예 막는데, 스크린샷은 그 한계를 쉽게 넘고 실패가 조용하다. 줌은 이미지를 넘길 때마다 초기화하고, 새 탭 버튼은 브라우저에서만 뜬다(JCEF엔 탭이 갈 곳이 없다). 방향키는 **위·아래도 좌·우와 동일**하게 동작한다. 제보로 잡은 버그 하나 더: **처음 여는 세션에서 클릭한 것과 다른 이미지가 열렸다** — 세션 색인이 뷰어를 연 뒤 도착하는데 `initialIndex`를 `useState` 초기화 함수로만 읽어 보정 위치를 버렸다(캐시가 생긴 두 번째 클릭부터 정상이라는 재현 조건이 원인을 그대로 가리켰다). 사용자가 이미 이동했다면 그 위치가 이기도록 해서 느린 fetch가 의도한 이동을 뺏지 않게 했다. **텔레메트리는 `ASSET_ACTIVITY` 한 타입에 `kind`를 실어** 게이트 노출·게이트 클릭·화면 열기(진입 경로별)를 기록한다. kind와 진입 경로를 property가 아니라 **이벤트 이름**에 넣는데, Rybbit이 이름별 고유 사용자는 주지만 custom property로는 사용자 필터가 안 되기 때문이다(실측). 전환율은 사람 수가 필요하고, 기본 숨김인 도크는 '안 쓴다'와 '못 찾는다'가 같은 숫자로 보이면 안 된다. **후속 보강**: 사용자 대면 문구를 전부 '이미지'에서 **에셋**으로 통일했다(12개 로케일) — 나중에 이미지 외의 것이 담겨도 이름을 다시 갈지 않기 위해서다. 화살표는 **갈 곳이 없어도 사라지지 않고 비활성으로 남고**, 카운터도 `1 / 1`까지 항상 그린다 — 장수에 따라 뷰어가 다른 화면처럼 보이던 것을 없앴다. 비활성 화살표에는 **툴팁으로 이유와 후원 링크**를 붙였는데, 툴팁을 버튼에 직접 걸면 **브라우저가 disabled 요소에 포인터 이벤트를 안 보내 영영 안 뜬다**(jsdom은 보내주므로 테스트도 통과해버린다). 그래서 래퍼 `<span>`에 건다. 게이트 문구는 두 벌로 만들어져 있던 것을 `SponsorGateNotice` 한 곳으로 합쳤다 — 에셋 화면 쪽 복사본은 링크도 텔레메트리도 없이 표류하고 있었다. **두 안내의 목적지를 갈랐다**: 카운터를 보정하는 '이 세션에 N장 더'는 **전체 보기**로 에셋 화면을 열고(그 N장은 이미 무료로 볼 수 있는 것들이다), 후원 링크는 '넘기고 싶다'가 명확한 **화살표 툴팁에만** 둔다. 따라서 `gate_clicked`는 후자에서만 나간다. 에셋 화면 헤더에 도크 아이콘과 **도크에 고정/해제 토글**을 넣었다 — 신규 도크 항목이 숨김으로 배포되는 규칙은 그대로 두고, 사용자가 도크의 존재를 배우는 입구를 만든 것이다. 비후원자 후원 페이지에는 후원자용 `SponsorBenefitsSection`을 **재사용**해 기능 목록을 노출한다. 문구 톤은 규칙 선언에서 '작은 혜택을 조심스럽게 내민다'로 바꾸고, **사과 표현을 쓸 언어와 쓰면 안 되는 언어를 갈라 적용했다**(ja·ko·ar·fa는 넣고, zh는 양해 요청으로, de·ru·en과 로망스어군은 넣지 않는다).
- [Copy a message you sent](./060-copy_a_message/en.md) — 내가 보낸 메시지를 **⋮ 메뉴의 "메시지 복사"** 로 복사한다([#412](https://github.com/Swttch/swttch/issues/412)). 일반 텍스트 메시지에서는 말풍선 옆 hover 복사 버튼이 **주석 처리돼 있어 아예 뜨지 않았고**, 손으로 긁는 것 말고는 방법이 없었다(IDE의 WebView에서 여러 줄 프롬프트는 특히 성가시다). hover 버튼을 되살리는 대신 **포크·되감기·응답 접기가 이미 모여 있는 ⋮ 메뉴**에 항목을 더했다. 한 가지 일을 하는 컨트롤이 두 자리에 나뉘어 있을 이유가 없다. 되감기와 달리 **조건 없이 항상 그려진다**(모든 메시지에는 화면에 뜬 텍스트가 있으므로 항목만 있고 안 되는 경우가 없다). 누르면 메뉴가 닫히므로 확인은 항목 위 체크가 아니라 **"메시지를 복사했습니다" 안내**로 준다(같은 메뉴의 포크·되감기가 결과를 알리는 방식과 동일). **함께 고친 것**: 슬래시 명령 말풍선에서는 그 버튼이 살아 있었는데 **빈 문자열을 복사하고 있었다**. CLI가 슬래시 명령을 태그 세 개로만 기록해서 태그를 걷어내면 평문이 남지 않기 때문이고, 이 컴퓨터의 세션 파일 실측 결과 슬래시 명령 45건이 전부 그 모양이었다. 말풍선으로 뜨는 것은 무엇이 만들었든 **보이는 대로** 복사되도록, 말풍선이 그리는 두 조각을 합치는 규칙을 함수 하나로 모았다. 두 경로가 모두 메뉴를 쓰게 되면서 `MessageActions`와 `useCopyToClipboard`, 그 둘만 쓰던 12개 언어의 번역 문구 두 개는 주인을 잃어 삭제했다 ([#412](https://github.com/Swttch/swttch/issues/412))
- [Dictation keeps running when an approval prompt appears](./061-dictation_survives_approval_prompts/en.md) — 받아쓰기로 다음 메시지를 부르는 도중 **권한 승인·계획 승인·질문 창이 뜨면 녹음이 그 자리에서 끊기던** 것을 수정([#409](https://github.com/Swttch/swttch/issues/409)). 원인은 승인과 받아쓰기가 부딪힌 것이 아니라 **녹음이 화면 조각의 수명에 묶여 있던 것**이다. 채팅 하단의 그 자리는 입력창 전용이 아니라 승인 창이 대신 차지하는 자리인데, 받아쓰기 세션이 입력창 안에 살고 있어서 입력창이 물러날 때의 뒷정리가 마이크까지 놓아버렸다(제보자는 말하던 것을 알아채고, 다시 켜고, 처음부터 다시 말해야 했다). 세션의 주인을 입력창에서 **채팅 화면 전체**로 옮겨, 승인 창이 떠 있는 동안에도 마이크가 열려 있고 승인을 처리하면 녹음이 끊긴 적 없이 이어진다. 받아쓴 글이 담기는 초안은 원래부터 입력창 바깥에서 관리되고 있었으므로 글자는 잃지 않으며, 커서가 없는 동안에는 **초안 끝에 이어 붙인다**(위치를 짐작해 끼워 넣으면 이미 적어둔 것을 덮어쓸 수 있어서, 끝에 붙이는 것만이 글자를 잃지 않는 선택이다). 같은 뿌리에서 고장나 있던 것 하나가 함께 풀렸다. **전역 단축키(`⌥D`)를 듣는 코드도 입력창 안에 있어서 승인 창이 떠 있으면 눌러도 아무 일이 없었다** — 화면 어디서든 동작하라고 만든 단축키가 정작 입력창이 없는 순간에만 사라지고 있었다. 입력창이 화면에 없는 동안에는 **승인 창 위에 알림줄**이 서서 목소리 크기 막대와 현재 상태와 중지 버튼을 대신 보여준다(녹음이 계속되는데 화면에 아무 표시가 없는 것은 차라리 멈추는 것보다 나쁘다). 침묵 자동 종료는 **늘릴 수 없다** — 전사 서비스가 15초 침묵이면 스스로 듣기를 멈추므로 우리 시계를 늘리면 화면만 녹음 중이라고 말하는 상태가 된다. 그래서 대신 **왜 멈췄는지 알리고 '이어서 녹음' 버튼을 준다(앞서 말한 것은 지워지지 않고 초안 끝에 이어 붙는다)**. 없앨 수 없는 제한이라면 최소한 보이게 만드는 것이 할 수 있는 최선이기 때문이다
- [A message from another session shows up without a reload](./062-peer_message_appears_live/en.md) — 같은 머신의 다른 Claude Code 세션이 `SendMessage`로 보낸 메시지가 **라이브 화면에는 아예 안 뜨고 세션을 다시 열어야 보이던** 것을 수정([#423](https://github.com/Swttch/swttch/issues/423)). 카드를 그리는 쪽은 [#383](https://github.com/Swttch/swttch/issues/383)에서 이미 만들어져 있었고, 없던 것은 **그릴 재료가 라이브 경로까지 오는 길**이었다. 처음 진단은 "CLI가 트랜스크립트에는 적지만 라이브 stdout으로는 안 내보내니 우리가 할 게 없다"였는데 **그 결론이 틀렸다.** 앞부분은 맞다 — 백엔드 로그 4개에서 stdout으로 흘러온 `type:"user"` 이벤트 270건이 전부 `tool_result`였고 `origin`을 실은 것은 0건이며, 이는 `claude-process.ts`에 이미 적혀 있던 실측("only `tool_result` entries come back that way")과 일치한다. 그러나 CLI는 **그 턴을 끝내는 `type:"result"` 이벤트에 같은 `origin` 객체를 `body` 전문까지 통째로 실어 보내고 있었다**(`server-2026-09-08T153322053Z.log` 10166·16730·16837행). 백엔드는 이미 모든 CLI 이벤트를 가공 없이 브로드캐스트하고 있었으므로 웹뷰는 그것을 **받고 있으면서 버리고 있었다** — `useChatStream.ts`의 result 브랜치가 `modelUsage`와 에러만 읽고 나머지를 무시했고, `origin`을 엔트리로 옮기는 코드는 이 이벤트가 절대 닿지 않는 user 브랜치에만 있었다. **재검증이 한 번 0건을 냈던 것이 이 이슈에서 가장 비싼 함정이다**: 그 JSON은 `origin`이 1070번째 문자, `"type":"result"`가 2336번째 문자라 필드 순서가 거꾸로여서, `RAW stdout.*"type":"result".*"origin"` 패턴은 0건이고 순서를 뒤집은 패턴은 3건이 나온다. `.*`가 순서를 강제한다는 사실 하나 때문에 "CLI가 안 준다"는 틀린 결론이 두 번 재생산됐다. **카드를 놓는 자리는 도착을 안 시점(턴 끝)이 아니라 그 턴이 시작된 시점**이다 — 도착 순서로 붙이면 카드가 자기가 유발한 답변 아래에 놓이고, 리로드하면 기록 파일 순서대로 그려져 위로 올라가므로 **같은 두 줄이 화면을 새로 열 때마다 자리를 바꾼다.** 시작 시각은 result가 함께 주는 `duration_ms`를 현재 시각에서 빼서 얻는데, 이렇게 하면 턴 시작을 기억하는 ref를 새로 두지 않아도 된다(상태를 늘리지 않고 파생으로 푼다). `kind === 'peer'`이고 `body`가 비어 있지 않은 것만 엔트리로 만든다 — 같은 자리로 `kind:"task-notification"`도 오는데 그쪽은 `body`가 아예 없어서, 거르지 않으면 plain user 경로로 새어 **빈 말풍선**이 된다. 엔트리의 `message.content`에는 `origin.body`를 그대로 싣는다. CLI 원본은 그 본문을 `<cross-session-message>` 태그와 "이건 사용자가 친 게 아니다"라는 안내문으로 감싸고 있는데, **그 래퍼는 CLI가 쓴 문장이지 우리가 추측으로 재현할 것이 아니기 때문**이다(렌더러는 어차피 `origin.body`만 읽는다). 타입에는 실측한 필드를 이름 그대로 추가했고(`verifiedPeerPid`·`msg_id`·`fromMode`·`hopChain`), `kind`는 **enum으로 좁히지 않았다** — 아직 못 본 값이 좁혀져 사라지는 것보다 통과하는 편이 낫다. 새 테스트 6개는 수정을 `if (false && ...)`로 무력화해 **정확히 3개가 붉게 뜨는 것을 확인한 뒤**에야 통과를 인정했고, 나머지 3개(task-notification·빈 body·origin 없는 result)는 무력화해도 통과하는 회귀 가드다. 검수는 워크트리 dev 서버에 브라우저로 붙어 **실제 GUI 세션을 만들고 다른 세션에서 그 세션의 소켓으로 실제 `SendMessage`를 쏘는** 방식으로 했다 — 리로드 없이 카드가 떴고, 세션을 다시 열어 대조하니 순서까지 동일했으며, **턴이 이미 돌고 있을 때 도착한 메시지도 동일하게 떴다**(중간에 "턴 중 도착은 안 된다"고 잘못 판단할 뻔했는데, 실제로는 그 턴이 아직 안 끝난 시점에 로그를 본 것이었다). **남은 한계 하나를 문서에 적어 뒀다**: 검수에서 보낸 4건 중 3건이 카드로 떴고 1건은 result에 `origin`이 실려 오지 않았다. 어떤 조건에서 빠지는지는 아직 모르며, 빠진 메시지도 트랜스크립트에는 남아 있어 **세션을 다시 열면 보인다**(그 경우 예전과 같은 상태가 될 뿐 더 나빠지지 않는다)
- [You can talk to a background agent](./063-talk_to_a_background_agent/en.md) — 백그라운드 에이전트를 **지켜보고 멈출 수는 있어도 말을 걸 수는 없던 것**을 열었다([#425](https://github.com/Swttch/swttch/issues/425)). CLI는 줄곧 그럴 수 있다고 알려주고 있었다 — 완료 알림의 `note`에 "the user can send it another message and resume it"이 들어 있는데, 그 필드가 백엔드에서 버려지고 있었다. **주소를 아는 쪽이 GUI뿐이라는 점이 이 기능의 핵심이다**: `workflow_agent`의 `agentId`를 CLI는 모델에게 돌려주지 않고(`ListAgents`에도 안 뜬다) 우리는 `task_progress`로 받는다. 실측으로 확인했다 — label로 보내면 거부되고 runtime `agentId`로 보내면 `Resuming agent …`로 성공한다. **한 번 반대로 결론냈다가 뒤집은 건이다**: 끝난 에이전트의 id로 보낸 실패를 "워크플로우 에이전트라서"로 읽었는데, 거절 문구는 `그 ID의 트랜스크립트가 이 세션에 없다`였고 실제로 세션 식별자가 바뀌어 있었다 — **거절 메시지가 말하는 범위보다 결론이 넓지 않은지** 물었어야 했다. 전달은 `<system-reminder>`로 감싸 모델에게만 보내고 채팅은 건드리지 않는다(취소 버튼이 쓰는 것과 같은 모양). `SendMessage`는 모델의 도구이고 **CLI에도 사용자가 직접 부르는 수단이 없어** 미문서화 우회를 하지 않았다. 보낸 메시지를 **우리가 저장하지 않는다** — 재개가 새 `task_started`를 `prompt`에 그 문장을 실어 쏘므로 `events.task_started[1..]`에서 되읽는다(두 벌이 어긋날 일이 없다). 입력란은 `InputFrame`을 **메인 채팅 인풋과 공유**한다 — 처음에 부품(`RichInput`·`ActionButtons`)만 빌리고 박스는 클래스를 복사해 다시 그렸다가 **폭도 높이도 placeholder도 어긋난 작은 네모**가 나왔고, 그 복사본이 커밋 전에 이미 틀렸다. 첨부·슬래시는 비활성이 아니라 **아예 그리지 않는다**(문자열만 받는 통로라 첨부는 갈 곳이 없고, 슬래시는 세션에 거는 것이다). **도달 불가 판정은 백엔드가 아니라 웹뷰에서 한다** — 처음엔 tracker의 `entries`를 뒤졌는데 **리로드로 복원된 태스크는 거기 안 들어가고**, 정작 거부당하는 것이 그런 태스크다(로그에 거부 9건·표시 0건). 대화에는 그 구멍이 없어서 `useUnreachableAgents`가 tool_result를 읽는다. 문구로 판별하고 `success:false`로는 안 한다 — 일시적 실패도 그것을 낸다. 정지 버튼은 **보내는 즉시** 뜨고(CLI 왕복을 기다리면 아무 일도 없는 것처럼 보인다) `agentId`로 그 에이전트만 멈춘다(워크플로우 전체가 아니다). 낙관적 표시에 만료를 둔 이유는 **재개가 렌더 사이에 끝나면 `running`을 한 번도 못 보고**, 워크플로우 에이전트의 재개는 아예 다른 태스크가 되기 때문이다. **같이 고친 것들**: ① 세션을 다시 열면 백그라운드 작업이 **하나도** 안 보이던 회귀 — `claude-process.ts`가 지운 바인딩을 세 곳이 계속 참조해 `isWorkflowRunning`이 throw했고, 호출부가 로그만 찍는 catch라 **복원 전체가 중단**됐다(서버 로그에 8건) ② `Agent`·`Bash` 태스크가 복원 스캔(`name === 'Workflow'`)에서 통째로 빠져 있던 것 ③ `usage`를 `{total_tokens→subagentTokens}`로 리네임하며 나머지를 버리던 것 — 원본 키로 되돌리니 `agents_done`·`agents_error`·`diagnostics`·`last_tool_name`처럼 **존재조차 몰랐던 필드들**이 살아 돌아왔다 ④ 평범한 `ls`가 백그라운드 패널에 행을 만들던 것(CLI가 `is_backgrounded: false`라고 말하는데 안 읽었다) ⑤ `task_updated` **179건이 통째로 무시**되던 것 — `task_id`로만 주소가 오는데 `task_progress`와 핸들러를 공유해 첫 줄에서 반환됐고, 그 안에 "실행 중 명령을 백그라운드로 보냄"과 "작업이 이렇게 끝남"이 들어 있었다(후자를 안 읽어서 출력 로그의 `[killed]`를 뒤지는 우회로가 생겼던 것) ⑥ 멈춘 작업이 화면에 `killed`로 나오던 것 — CLI가 같은 사건에 patch는 `killed`, notification은 `stopped`로 두 단어를 쓴다. **검수는 브라우저에서 전부 실측했다**: 새 세션(`/sessions/new`)에서 처음부터 띄워 `ok` 전달(`Message queued for delivery…`), 같은 세션의 워크플로우 에이전트에 `yo` 전달(`Resuming agent a07005b`), 이전 세션의 워크플로우에서 입력란이 잠기고 붉은 사유가 뜨는 것, 완료 후 버튼이 전송으로 복귀하는 것까지. **테스트를 믿지 않고 무력화로 검증했다** — 한 번은 `useEffect`를 지워도 테스트가 통과해서 **그 코드가 애초에 불필요했다**는 것을 알았고(다른 조건이 이미 그 일을 하고 있었다), 지우고 조건 하나로 합치니 그제서야 무력화가 정확히 실패시켰다
- [Usage stats work behind a proxy](./063-usage_behind_a_proxy/en.md) — 프록시를 거쳐야만 인터넷에 나가는 환경에서 **사용량 패널만 실패하던** 것을 수정([#181](https://github.com/Swttch/swttch/issues/181), 제보자가 직접 [#432](https://github.com/Swttch/swttch/pull/432)로 기여). 채팅은 되는데 사용량만 안 되는 이 갈림이 원인을 알려준다 — 채팅은 `settings.json`을 스스로 읽는 `claude`가 내보내지만, 사용량 수치는 **자격증명을 읽어야 해서 플러그인 밖으로 뺀 `ccb`**가 가져오고, 그쪽은 그 파일을 읽지 않았다. 고칠 곳이 **두 군데로 갈렸다**: `ccb`(`@swttch/extend-kit` 0.6.0)는 환경변수를 받아도 쓰지 않고 있었고(Node의 전역 `fetch`가 `HTTP_PROXY`를 무시한다), 백엔드는 `settings.json`의 프록시를 `ccb`에게 건네지 않고 있었다. **한쪽만 고치면 아무 변화가 없다.** 백엔드 쪽은 스폰 지점마다 설정을 읽는 대신 `CLAUDE_CONFIG_DIR`·`cliPath`가 이미 쓰던 방식대로 **컨텍스트 로드 시 `process.env`에 투영**한다 — 네 스폰 지점 중 둘(`fetchAccountUsage`, 서버 기동 시 한 번 등록되는 auto-resume 훅)은 애초에 `workingDir`를 가질 수 없어 조용히 전역 설정으로 폴백했기 때문이다. 투영이 낳는 위험(프로젝트를 옮겨도 프록시가 눌어붙는 것)은 시작 시 상속한 값을 기억했다가 되돌리는 것으로 막고, 키별로 독립 판단한다. HTTP·HTTPS와 **SOCKS(`ssh -D` 터널)**를 모두 지원하며 음성 입력의 WebSocket도 같은 프록시를 지난다. 검증에서 배운 것: **프록시는 "요청이 성공했나"로 판정할 수 없다** — 목적지에 직접 닿을 수 있으면 프록시를 건너뛰어도 성공하므로, 실제 CONNECT 프록시를 띄워 **그 프록시가 터널을 봤는지**로 판정한다(원래 피알이 통과하면서도 아무것도 바꾸지 못한 이유가 정확히 이것이다)
